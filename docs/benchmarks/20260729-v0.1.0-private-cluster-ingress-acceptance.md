# SeqEvi 0.1.0 Private Cluster Ingress Acceptance

> Status: accepted deployed evidence
> Date: 2026-07-29
> Issue: FUQ-22

## Scope

Acceptance covered the installed system Apache boundary, the stable loopback
SeqEvi service, PostgreSQL-backed evidence persistence, and the accepted
SeqEvi image as a worker context. The reviewed configuration from merge commit
`d82eeb2042d2a0949ba3c2ad7aaab5ab51b738d5` is installed at
`/etc/httpd/conf.d/seqevi-store.conf`.

The deployment was validated before reload, after reload, and after a complete
Apache restart. The final worker run exercised health discovery, an existing
cache hit, artifact downloads, and one controlled miss-to-commit-to-hit cycle
through the private listener.

## Runtime Evidence

| Property | Observed value |
| --- | --- |
| Host | `node4.cluster.local` |
| Existing port conflict | `18080` already listening on `0.0.0.0` and `::` |
| Installed private listener | `192.168.30.205:18082` |
| Installed configuration digest | `bd861ff8585e57ea3c8b0d5d7b2b00d3ae9062767b639862db830af3c0cc5dd0` |
| Complete Apache config test | `Syntax OK` |
| Apache restart | successful; active since `2026-07-29 16:47:22 CST` |
| SeqEvi upstream | healthy at `127.0.0.1:18081` |
| PostgreSQL | listening only at `127.0.0.1:15432` |
| Allowed worker context | ordinary Docker bridge container |
| Allowed worker source | `172.17.0.10` |
| Allowed worker health | HTTP `200` |
| Negative worker source | temporary `172.31.0.2` |
| Negative worker health | HTTP `403` |
| Direct worker access to `192.168.30.205:18081` | rejected |
| Old password file | absent |
| Old SeqEvi certificate and key | absent |
| Negative-test Docker network after cleanup | absent |

The negative network was deliberately outside both configured ranges:
`172.17.0.0/16` and `192.168.30.0/24`. Apache used the socket source address;
client-supplied forwarding headers are removed before proxying.

## Existing Store API Read

The allowed worker crossed the proxy for health discovery and fetched one
previously accepted immutable `eggnog/1` hit. It then downloaded and
digest-verified both referenced artifacts:

| Property | Accepted value |
| --- | --- |
| Payload digest | `ebb7560bfe6ae043bf873e00b8ed3c366a7ea8c5ec1063ff974d9ce597dd372b` |
| Normalized artifact | `b14140244a07630a0ee2c154d4673ec124a928ed3c26c04386d0b966f9b79ce2` |
| Raw artifact | `2680182679a7946c59f342711f3e2e3dcecde8a0fcf7b5c814529657c1ccc706` |

This exercised `/health`, `POST /v1/evidence/fetch-many`, and both artifact
downloads through the installed plain HTTP private ingress.

## Controlled Store Write

The allowed worker used the public `HttpEvidenceStore` client with a dedicated
acceptance identity. Its first exact lookup was a miss, its immutable commit
returned `created`, and its second lookup and fetch returned the committed
`no_hit` record and digest-verified raw artifact.

| Property | Accepted value |
| --- | --- |
| Adapter contract | `seqevi-ingress-acceptance/1` |
| Resource identity | `acceptance/FUQ-22/2026-07-29/d82eeb2` |
| Sequence ID | `SQ.8ymbkuNHWrhUsVUmOcJFeDTjbIFNl6_E` |
| Commit outcome | `created` |
| Payload digest | `d250837673f284f93f348ca709f458043c054d94f5c5a5aefbcf30226228ece4` |
| Raw artifact digest | `d250837673f284f93f348ca709f458043c054d94f5c5a5aefbcf30226228ece4` |
| Lookup after Apache restart | hit |
| Artifact after Apache restart | digest and bytes verified |

The dedicated contract and resource labels keep this operational evidence
separate from eggNOG and InterPro scientific results. The record is retained
as immutable restart-persistence evidence.

## Trust Boundary

The allowed CIDRs are the complete authorization boundary. The Store API also
supports artifact upload and evidence commit, so every process in those ranges
is trusted as a Store writer. The API's digest, schema, size, and immutability
checks remain active but do not identify the caller.

## Result

FUQ-22 is accepted. The installed Apache boundary provides the required
cluster reachability while SeqEvi and PostgreSQL remain loopback-only. The
previous TLS, password, and secret-file design is not installed; its exact
candidate files were confirmed absent. The temporary negative-test Docker
network was removed after the HTTP `403` assertion.

# dbCAN D3 Target-Store Replay Acceptance

Version: v1.0
Date: 2026-08-19
Status: accepted all-present target replay and independent replay

The fixed 50-record input is an exact subset of the accepted D4 input. Its
live D3 inventory was therefore honestly classified 50 present / 0 missing
after D4. D3 did not perform or claim a cold 50-record computation.

The input SHA-256 is
`e595adb96e198cabf30fda34adf13d25d3697df42861e60dba3c22a0192a616a`;
its EvidenceKey-set SHA-256 is
`79a2b436d1f1134a31c44e4c4692ae8224eb1172d9be56129d75e3743d39e486`.
Both runs used the exact D4 execution identity, accepted immutable local image,
64-thread declaration and finite 7,200-second managed OCI timeout. The public
GHCR image and ordinary profile remained absent.

The first managed replay completed in 9.56 seconds wall time; an independent
later process completed in 9.06 seconds. Each reported 50 cache hits, zero
computed, 5 hits and 45 no-hits. Each schema-correct DuckDB contains 50
sequence-map/annotation rows, 5 native evidence rows and 45 no-hit rows. The
2,109,440-byte output SHA-256 values are respectively
`bf41dc86c8edff1078d71ee45474b40fa923e38f2abf1f7ab7d24a5b8ae8bf62`
and
`a33161f86029000e4c3ec0861373fcc126654362277fdd31d63834977173cd7d`.

Across both runs, sequence, evidence and artifact row deltas were zero; CAS
file and byte deltas were zero. After each fixed retention window, final active
ClaimSession and receipt rows were zero. The final Store remained healthy at
revision `0004_claim_sessions` with 53,720 sequences, 56,450 evidence rows, 35
artifacts and 20,565,520 artifact bytes. The 35-file CAS inventory was
unchanged. Resource metadata, public identity absence and persistent
deployment identity were unchanged; no AO-owned or invocation-owned container
remained.

Raw logs and complete readbacks remain in the isolated evidence root. D3 is
accepted as target-Store cache reuse, not repeated direct-versus-managed parity
or a cold target computation. No D5 same-key process was launched.

# InterPro Preflight Containment Harness Acceptance

Version: v1.0
Date: 2026-08-19
Status: accepted harness prerequisite; InterPro read-only Preflight next

## Outcome

The repository-owned eggNOG/InterPro acceptance launch boundary now requires
two distinct finite deadlines: an internal adapter timeout passed to
`seqevi annotate --timeout-seconds` and a strictly longer external watchdog.
The benchmark-only wrapper delegates process ownership to the existing product
`ToolRunner`; it does not duplicate or weaken that contract. Its CLI shim
converts outer-watchdog TERM into stack unwinding through `seqevi annotate`, so
the unchanged inner adapter `ToolRunner` synchronously cleans its separately
created process group before the outer boundary can finish. `ToolRunner`
launches an argument array without a shell in a dedicated session/process
group, keeps stdout and stderr in independent roots, sends TERM, waits a fixed
grace no longer than 60 seconds, sends KILL, waits/reaps the leader, and returns
only after repeated `/proc` readback proves no live group member and no
ambiguous process state.

The frozen eggNOG C2 harness values are a 21,600-second internal timeout,
21,660-second external watchdog, and 30-second termination grace. These values
are harness safety configuration only and do not alter accepted scientific
inputs, identities, parameters, results or Store evidence. InterPro must freeze
its own internal and longer watchdog values during read-only Preflight before
any annotation is authorized.

No annotation, Store/CAS mutation, image pull, or eggNOG C1/C2 rerun occurred
for this acceptance.

## Frozen Source Lineage

| Boundary | Frozen value |
| --- | --- |
| harness source commit | `6250537dba57723f3aaacc0207e2290638b17e04` |
| containment helper Git blob / file SHA-256 | `749f6f841d46262fd8f6d6c8b6cc0cad9bc6e591` / `dbdd08c1758b9176d252247babf555b655f5d0077cdeb593f2270c01506f4e73` |
| watchdog-to-CLI bridge Git blob / file SHA-256 | `346fb81d0767666ff547ca773aec1b45bb7179a7` / `e8a39c229dd0d175fa140876be4eb5db85e1f7d0f23ac631773571d7741b295c` |
| C2 integration Git blob / file SHA-256 | `55287fff7aad6a6ab9630090d4447d337928f6db` / `ec7de11d5ab48931527f9baf9523a74b186e091c3c42b05e844e412f8b170de7` |
| focused failure-path test Git blob / file SHA-256 | `fee7687e1494a2a70124d9dc107b2c02e24f3cec` / `02a57df7b91536e1615f94e00c3b383dba7ba61ff20632ff57029cbf6bc7e810` |
| acceptance-boundary test Git blob / file SHA-256 | `02649470ef3e6f7e9a2967af5e5b2a364af4c0d9` / `90514bee533ab62e2a3cd0d8e5fdddb366ec603ed1bf597c7de01ef7ca90dbc7` |
| unchanged ToolRunner Git blob / file SHA-256 | `bfc1abb114863938fcd86dd50060500f31de3144` / `089744f5ee6d61c25d3c4a28e07c722fc715621d222fdd5ec67c5e7fd326ea5b` |

## Focused Failure-Path Evidence

The focused command was:

```bash
pdm run pytest tests/test_acceptance_containment.py \
  tests/test_acceptance_harnesses.py tests/test_runner.py -q
```

It passed 78 tests. The new tests launch real Python processes rather than
accepting normal-exit scans as timeout evidence:

- a hanging leader and TERM-resistant descendant exceed the external
  watchdog; observed signals include TERM and KILL, both TERM handlers run,
  the leader is reaped, and `/proc` readback finds no live member in the owned
  process group;
- a leader waits boundedly for its descendant's handler-ready marker and then
  exits before that TERM-resistant descendant; cleanup still observes the
  child's TERM handler, kills/reaps as applicable, and leaves the owned group
  empty;
- timeout and cancellation become stable completed-error states: repeated
  `poll()`/`wait()` calls return the same terminal code, while cleanup reads
  and returns the original typed error without an unbounded retry;
- a real outer watchdog expires while an inner `ToolRunner` owns a separate
  session containing a hanging leader and descendant; TERM unwinds through
  the benchmark CLI bridge, both nested TERM handlers run, the inner leader is
  reaped, and `/proc` proves the nested group empty; and
- the real C2 child shape starts the exact bridge file under `python -P` with
  `PYTHONPATH` containing only the candidate `src`; `--help` reaches the
  ordinary SeqEvi CLI without importing `benchmarks` from repository-root
  path state, while the initial and replay constructors share that command;
  and
- non-finite/non-positive deadlines, a watchdog not longer than the internal
  timeout, grace above 60 seconds, and CLI/internal-timeout mismatch all fail
  closed.

The repository canonical gate is recorded by the delivery PR at its exact
documentation head. InterPro Preflight remains read-only and must retain this
accepted code/test lineage; a semantic harness change invalidates reuse.

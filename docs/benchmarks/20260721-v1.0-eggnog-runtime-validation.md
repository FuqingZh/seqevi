# eggNOG-mapper 2.1.13 Runtime Validation

> Status: passed
> Date: 2026-07-21; runtime identity strengthened 2026-07-22

## Contract

This validation closes the Phase 4 external-runtime gate for the `eggnog/1`
adapter using:

- eggNOG-mapper `2.1.13`;
- eggNOG database `5.0.2`;
- DIAMOND `2.1.15`;
- the fixed `eggnog/1` protein, DIAMOND, taxonomic, GO, and Pfam parameters;
- 10 deterministic mouse protein sequences identified by SeqEvi `SequenceID`.

The runtime was an explicit-package micromamba clone of the accepted source
environment. The expanded native database was the versioned eggNOG 5.0.2
resource.
The source and cloned environments produced identical 289-package explicit URL
lists.

## Results

| Run | Result | Wall time | Peak RSS |
| --- | --- | ---: | ---: |
| Direct eggNOG-mapper | 10 annotation rows | 54:32 | 4,602,248 KiB |
| SeqEvi first run | 0 cached, 10 computed | 59:24 | 4,602,372 KiB |
| SeqEvi second run | 10 cached, 0 computed | 9:59 | 218,896 KiB |
| Adapter initialization, warm filesystem cache | resource/runtime identity | 2:24 | 68,256 KiB |

The direct annotations, first SeqEvi `evidence.parquet`, and cached SeqEvi
`evidence.parquet` were semantically identical: 10 rows, 22 normalized columns,
and no no-hit records. Both Parquet files had SHA-256
`95830e154894db21392fc6404ee4ee9cf48c7b60a9fdc674c82818d275b588f8`.
The first `sequence-map.tsv` marked all 10 sequences as `computed`; the second
marked all 10 as `cache`.

The original Phase 4 evidence identity was:

- resource: `eggnog/5.0.2/sha256:53da5d7e6c04fd944e1eb03b4a18b85bc98c45138da2321f5c2e82701c58fb5e`;
- runtime: `sha256:e9b23d13739b295429830d4c341d0ff37545411c5d381c932bf397c080f79b44`.

Phase 6.2 intentionally replaced the launcher-only runtime fingerprint. Against
the same installed eggNOG-mapper 2.1.13 package and DIAMOND 2.1.15 environment,
the complete path-independent runtime digest is
`sha256:b4e944c10e0d650295ba258405179ac69a7fdb3cc810ee4337e087a48e6e84a8`.
It covers the launcher, installed mapper package files and DIAMOND binary.
Evidence under the previous digest remains immutable but is not reused by the
strengthened contract.

## Findings

Real-runtime validation found and corrected three fixture blind spots:

- version probing must pass the selected `--data_dir`;
- the runtime `bin/` directory must lead `PATH` for `#!/usr/bin/env python` and
  sibling search tools;
- eggNOG-mapper v2 uses `-i` and `-m`, not `--input` and `--mode`.

The validation exposed two Phase 6 performance constraints. The adapter fixes
`--cpu 1`, making a small full-database run compute-bound, and the old
constructor rehashed about 50 GB of native database files on every invocation.
A dedicated warm-cache constructor run took 2:24.20; the full cache-only wall
time also includes lookup and package materialization. Phase 6.1 now provides
`seqevi.lock` and explicit full verification so a locked snapshot avoids those
repeated content reads. A real-database lock timing will be recorded when that
mount is available. Thread tuning remains deferred until the bounded-memory
dataflow is complete.

Raw smoke artifacts and command logs were task evidence outside the repository;
the stable inputs, checksums and conclusions are retained here.

# ClaimSession Cancellation and Contention Acceptance

> Status: accepted
> Date: 2026-08-17
> Issue: [#37](https://github.com/FuqingZh/seqevi/issues/37)

## Decision and exact-head lineage

This record accepts generic external-tool cancellation, ClaimSession
authority-loss wiring, Linux ordinary process-group cleanup, PostgreSQL
pressure, and the real eggNOG C2 gate. The cold C2 ran at frozen implementation
candidate `9d417da43c14e16cd2766870dfe11a5485e84937` (tree
`c7a30af4374ac98361e20e56662400356a3b9cb5`). A later pressure-harness-only
guard requires every relevant Store table to be empty before measurement. The
replacement pressure run passed at exact clean candidate
`7edc5e72636c20b228f8608a6ae6ce5e0135d349` (tree
`ec55a167069a1a380910796d166dbd47946a122f`). Between those candidates there
were no runtime, Store, CLI, C2 harness, adapter, or scientific changes, so the
9d417da cold C2 remains the authoritative C2 evidence.

The commit that updates this record after the replacement pressure run is an
evidence-only successor. It does not claim either run executed at the later
documentation SHA.
Evidence for candidate `2897dda079f3a4d432ae681029409b76beeda43b`
remains historical and is superseded for current acceptance by the fresh
2026-08-17 reports below. The rejected `4414f97` and `21c04aa` pressure roots
and their databases were retained separately and were not reused.

The candidate blobs used by the acceptance boundary were:

| Surface | Git blob |
| --- | --- |
| `src/seqevi/runner.py` | `bfc1abb114863938fcd86dd50060500f31de3144` |
| `src/seqevi/annotate.py` | `ac46d053df8af7fbd40188f0f4390078e5872004` |
| `benchmarks/c2_acceptance.py` | `221b9a4910bcae7231e254c00527f3f109c2c0d4` |
| `benchmarks/claim_session_pressure.py` | `0e8ca2653c833db3b0b6e33785a42e1e9c7c92d8` |

## Real C2 acceptance

The frozen caller-owned inputs used profile `eggnog-5.0.2`, the approved
expanded eggNOG 5.0.2 resource, and two simultaneously launched annotation
processes configured with 64 threads each. The BLF input contained 9,116 unique
evidence identities; the UniProt proteome contained 9,115. Their union was
9,116 and overlap was 9,115.

| Input | SHA-256 | Initial result | Independent replay |
| --- | --- | --- | --- |
| BLF | `9dc23bc3d230e097243110ac8e3a77df3e7f69c181d29290ed1d8d20e3e268d5` | 1 computed, 9,115 cache hits; 8,777 hit, 339 no-hit | 9,116 cache hits, 0 computed |
| UniProt | `a21f7da241177ebc75ab182e6ad77ff974ed4d21c41807953e0535266f0a7509` | 9,115 computed, 0 cache hits; 8,776 hit, 339 no-hit | 9,115 cache hits, 0 computed |

Both initial processes exited successfully. Their aggregate initial invariant
was exactly 9,116 computed, 9,115 cache hits, and zero observed
`existing_finalizations`. Initial wall time was 4,378.48 seconds; independent
replays completed in 87.73 seconds. Final PostgreSQL readback found exactly
9,116 evidence rows, evidence-identity digest
`8a522772589206c384717bb75064048ef3e80973228d757caa8d6c5f3342ab8d`,
zero dangling artifact references, zero ClaimSessions, zero session claims,
and zero open receipts. The candidate-owned Store was stopped and reaped, and
no matching live eggNOG or DIAMOND process remained.

The authoritative report is
`/home/fqzhang/tmp/seqevi-37-acceptance-20260817-9d417da-final3/c2/acceptance.json`
(SHA-256
`22dd9204c943259fef3a055bfee6b3c3e02dcaba376eae0fe145287ab5ee5385`).
It records the candidate source/import path, exclusive candidate `PYTHONPATH`,
redacted Store command, fixed input digests, profile `eggnog-5.0.2`, 64 threads
per process, `source_head=9d417da43c14e16cd2766870dfe11a5485e84937`,
and a 121.74-second observed retention wait. The Store used the fresh database
`seqevi37_c2_9d417da_final3`, loopback port 18090, and artifacts below the fresh
output root.

The command was:

```text
PYTHONPATH=src:. pdm run python benchmarks/c2_acceptance.py \
  --blf /home/fqzhang/tmp/fasta/others/Trichoderma_reesei_BLF.fasta \
  --uniprot /home/fqzhang/tmp/fasta/others/Trichoderma_reesei_uniprotkb_proteome_UP000008984_2025_10_27.fasta \
  --profile eggnog-5.0.2 --store http://127.0.0.1:18090 \
  --database-url postgresql+psycopg://<redacted>@127.0.0.1:15437/seqevi37_c2_9d417da_final3 \
  --threads 64 \
  --output-root /home/fqzhang/tmp/seqevi-37-acceptance-20260817-9d417da-final3/c2
```

## PostgreSQL pressure acceptance

The guarded repository harness first proved every relevant Store table empty,
then ran against fresh database `seqevi37_pressure_7edc5e7_freshguard` on
PostgreSQL 17.10 with
`max_connections=300`, `shared_buffers=512MB`, and two concurrent workload
clients. Counts were 100, 1,000, 3,000, and 9,116 cold claims. Heartbeat renew
overlapped acquire and finalize throughout. Because this harness exercises
production persistence directly, HTTP status observations are explicitly
`unavailable-direct-persistence`, not fabricated zero counts.

| Claims | Renew calls | Renew max | Acquire max | Finalize max | Terminal evidence |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 100 | 3 | 0.233 s | 0.250 s | 0.189 s | 100 |
| 1,000 | 7 | 1.568 s | 1.496 s | 1.623 s | 1,100 |
| 3,000 | 28 | 1.922 s | 2.136 s | 1.876 s | 4,100 |
| 9,116 | 159 | 3.277 s | 2.287 s | 3.069 s | 13,216 |

Every renew used exactly five SQL executions. Each at-most-1,000-key acquire
used 19 and each finalize used 15; close used five. Maximum observed pool
checkout wait was 11.797 ms. Activity sampling
collected 14,535 samples, observed at most three database sessions, two active
sessions and one lock waiter, and recorded no
heartbeat failure or authority loss. HTTP `503`/`412` observations remain
unavailable in this direct-persistence gate. Immediate close retained the four
protected open receipts as designed. Identity-scoped current-lane
readback was exact at every count while retention-aware global observations
allowed older rows to have been swept. In this run all protected coordination
rows remained until retention. After the 121-second retention wait, 14
work-returning bounded sweeps plus the terminal empty pass completed removal of
all 13,216 receipt items, 15 acquire headers, four open receipts, and four
sessions. Cumulative terminal evidence was exactly 13,216. Final
readback was zero for sessions, claims, open receipts, acquire receipts, and
receipt items.

The authoritative pressure report is
`/home/fqzhang/tmp/seqevi-37-acceptance-20260817-7edc5e7-freshguard/pressure/report.json`
(SHA-256
`5ccb128b5e991483732bcd7364ee90ffb00cb47a609cc4cd9fb9feecf0a3012f`).
It records candidate head `7edc5e72636c20b228f8608a6ae6ce5e0135d349`.
The command was:

```text
PYTHONPATH=src:. pdm run python benchmarks/claim_session_pressure.py \
  --database-url postgresql+psycopg://<redacted>@127.0.0.1:15437/seqevi37_pressure_7edc5e7_freshguard \
  --report /home/fqzhang/tmp/seqevi-37-acceptance-20260817-7edc5e7-freshguard/pressure/report.json
```

## Focused fault coverage

Repository tests exercise direct event wake before process discovery, the
sub-second cancellation contract, timeout, exit/cancel and termination races,
SIGTERM-resistant and normally orphaning descendants, final group fencing,
subreaper-aware exact-PID reap, `cleanup_stuck`, `KeyboardInterrupt`, renew
transient/exhausted `503`, renew `412`, slow-renew timeout, session close
failure, and the no-finalize/no-success authority-loss boundary. This remains
generic runner behavior: it adds no Store, HTTP, adapter, CLI-timeout, DBAPI
proxy, scheduler, or OS-uninterruptible-I/O guarantee.

# Managed dbCAN Release-Equivalent Candidate Gate

> Status: blocked at exact-input transport; scientific gate not run
> Date: 2026-08-06
> Issue: [#16](https://github.com/FuqingZh/seqevi/issues/16)

## Decision

The GHCR publication record remains accepted distribution evidence. Node4's
daemon-side GHCR transport is a non-blocking site issue. The scientific gate
may instead use an immutable Linux/amd64 local candidate built from the exact
published source, Dockerfile, locks, pinned Python base digest and SHA-verified
inputs. The candidate is identified by local image ID and never masquerades as
the public digest. Public setup and profile behavior remain unchanged and
expose no image override.

Direct and managed runs using that same candidate must have equal
`ToolRuntimeDigest`. A separately installed host runtime may have a different
exact fingerprint, but must retain equal scientific results, parameters,
`ResourceID`, hit classification, normalized values and public DuckDB schema.

## Attempted Candidate

The uncommitted evidence root is
`/home/fqzhang/tmp/seqevi-dbcan-managed-gate-20260806-LFnc3N`. The requested
long revision omitted one character; Git and GitHub resolve published PR #12
to `7f8b4d9dbaea1e3aebbb6c25f8e16738ea19cde7`.

| Input | Identity |
| --- | --- |
| source | exact Git archive of `7f8b4d9dbaea1e3aebbb6c25f8e16738ea19cde7` |
| wheel | SHA-256 `fea23432868f4547b2167eb5e0c7753a9b123e6d3d131c3f6a33eca98a5ec08e` |
| wheel provenance | all 38 embedded SeqEvi Python files byte-equal to the archive |
| base | `python:3.13.11-slim-bookworm@sha256:ac76900038d8606cc99b413d4ede77bc7152f1e42b94cf5d50d4b80a999652fe` |
| platform | `linux/amd64` |
| build definition | archived Dockerfile, hash locks, `inputs.json` and `fetch_inputs.py` |

An isolated digest-pinned BuildKit container used host networking and the
caller's proxy solely as transport. BuildKit resolved the exact Python base.
Both locked Python environments installed, the archived wheel installed, and
both `pip check` probes passed.

## Exact Blocker

The first unchanged build failed in `fetch_inputs.py` with a TLS handshake
timeout while fetching the five SHA-pinned dbCAN/DIAMOND inputs. One authorized
retry reused the exact cache and repeated only that release step. It failed:

```text
http.client.IncompleteRead: IncompleteRead(14172160 bytes read)
```

No alternate input was used. The retry budget was exhausted, the temporary
builder was removed, and no candidate image exists. Therefore image versions,
compliance contents, database absence, real results, runtime digest equality,
mount proofs, resource-write proof and the required 50-hit/0-computed replay
are not claimed.

The approved FASTA was present with SHA-256
`e595adb96e198cabf30fda34adf13d25d3697df42861e60dba3c22a0192a616a`.
The exact caller-owned resource subtree contained the four locked database
files and `seqevi.lock`; it was enumerated only at depth one and not mutated.

## Resume Condition

Resume only when all five exact `inputs.json` artifacts can be transported
completely with their declared SHA-256 values. Do not substitute a base or
input. Then run the direct baseline, managed cold run and separate
later-process replay, requiring 50 cache hits and 0 computed records.

# dbCAN Target-Store Execution Plan

Version: v1.0
Date: 2026-08-19
Status: accepted dbCAN closeout; InterPro Preflight next
Target release: SeqEvi 0.3.1
Issue: #55
Continues: [Shared Store cache-seeding implementation
plan](20260807-v1.0-shared-store-cache-seeding-implementation-plan.md)

## Decision And Scope

Continue the master cache-seeding plan after the accepted eggNOG lane with a
managed dbCAN target-Store lane before InterPro. This dated plan supersedes
only that active next-lane ordering and the master plan's generic dbCAN gate.
It does not rewrite accepted eggNOG evidence, close global cache seeding, or
change any architecture, adapter, Store, evidence-identity, result-schema, or
managed-distribution contract. InterPro resumes only after dbCAN closeout.

The execution target is one managed dbCAN process with `64` adapter threads
for both the fixed 50-record target gate and the 2,729-record sizing run. There
is no `8/32/64` sweep. Real file concurrency starts and remains at one unless
the optional post-sizing two-file gate below is explicitly selected and its
measured headroom gate passes.

## Concurrency Vocabulary

These are independent axes and must not be reported as one undifferentiated
"concurrency" value:

- **per-adapter threads** are the `--threads` value passed to one dbCAN
  invocation. dbCAN passes the same value into DIAMOND and PyHMMER, but its
  three selected analyses execute sequentially;
- **simultaneous SeqEvi or file processes** are independent caller processes,
  each with its own project root, DuckDB result, managed container and thread
  allocation; and
- **ClaimSession same-key coordination** is Store-side coordination between
  callers requesting the same EvidenceKeys. It is not equivalent to either
  adapter threads or simultaneous managed containers.

Current SeqEvi 0.3.1 managed-v2 dispatch creates and starts one OCI container
for every caller before the inner SeqEvi process opens its ClaimSession. It has
no claim-before-OCI path. Consequently a `two callers at 64 threads` gate that
requires the waiter to launch no container is impossible on this release. D5
records that limitation as unavailable and makes no same-key managed-gate
claim. A future gate requires a separately reviewed claim-before-OCI product
path; this plan neither invents that path nor substitutes proof that the waiter
skipped only the inner dbCAN batch.

## Fixed Identities And Endpoints

| Boundary | Fixed value |
| --- | --- |
| client and service | released `seqevi==0.3.1` |
| managed kit | `dbcan-cazyme-5.2.9-db-2026.05.05` |
| OCI image | `ghcr.io/fuqingzh/seqevi-dbcan@sha256:051cbffa57b1b39fa8a6f811832dcc3224671b4a378ef8df7000f7f4314db9b6` |
| ResourceID | `dbcan/db_v5-2-9_5-5-2026/sha256:33c60f3492f39156a2929589af5af1c493f3dac17739ee7467e3024e28a4857f` |
| caller-owned resource | `/cephfs_data/genostack_v3/genostack_php/public_file_data/database/bioinfo/resources/dbcan/database/db_v5-2-9_5-5-2026/raw` |
| host Store inspection | `http://127.0.0.1:18081` |
| managed-container Store | `http://192.168.30.205:18082` |
| adapter threads | `64` per managed dbCAN process |
| initial file concurrency | `1` |
| result schema | exact adapter-native `dbcan-cazyme/5` surface |
| adapter contract | `dbcan-cazyme/1` |

The host endpoint is used for read-only operator inspection. The bridge
endpoint is the Store URL supplied to the managed container. Generic health
and ClaimSession capability responses cannot prove that the addresses are
aliases. Before setup apply or annotation, D1 therefore requires
deployment-specific authoritative mapping from both listeners through their
proxy/NAT/service configuration to one exact service instance, database and
artifact Store, plus a read-only database/Store identity readback from that
deployment authority. Equal API payloads alone do not pass. Neither address is
a product default.

### D2 Local-Candidate Recovery Addendum

The public GHCR digest remains the release and managed-kit identity, but
acquiring it is transport evidence rather than a scientific prerequisite for
this execution lane. On 2026-08-19, supported released `seqevi==0.3.1` setup
apply attempts 1 and 2 each reached the finite 900-second fixed-digest pull
timeout. Authoritative readback after each attempt proved the public image,
profile and pull/setup processes absent; invocation-owned container residue was
zero and Store, CAS and resource state were unchanged. After the required
60-second recovery backoff and single retry were exhausted, the user approved
a release-equivalent local candidate instead of another public pull.

The local candidate must be built from published SeqEvi `0.3.1` source revision
`da55c14d95ef61140d3fa9c047df333aaea8798c` using the repository release
Dockerfile, both hash-locked requirements files, `inputs.json`, its pinned
base-image digest and only SHA-verified upstream inputs. Retain the source
commit, clean-tree readback,
Dockerfile/lock/input digests, wheel digest, build argument array, base digest,
input verification, OCI labels and final Docker inspection. The candidate is
identified only by its immutable local `sha256:<64hex>` image ID. A mutable tag
may be a build convenience but is never execution identity, is not written to a
profile and is not represented as the public GHCR digest.

Production setup, kit, profile and CLI behavior remain unchanged. The ordinary
profile continues to contain only the public kit image identity; no production
image/profile override is added. Execution uses the repository-owned
`benchmarks/dbcan_local_candidate.py` in-process validation harness. It first
requires exact public profile/kit agreement and an authoritative Docker inspect
readback of the immutable local ID, then substitutes that ID only for the
single image token at the OCI `docker create` boundary. The harness must restore
the production functions after each invocation, never pull an image and remain
a single-process benchmark boundary rather than a public entrypoint.

For recovered D2, repeat the supported dry-run as transport/contract evidence;
record the failed public applies and the ordinary profile's resulting state.
Then use the local candidate for the same ephemeral read-only smoke and the
separate released-adapter runtime-identity probe. Freeze the resulting
ToolRuntimeDigest, independently rederive ResourceID and inventory the 50 and
2,729 EvidenceKeys exactly as originally required. Scientific D2 acceptance
depends on candidate provenance, smoke, runtime identity, inventories, unchanged
Store/CAS/resource state and zero residue—not on a successful public pull.

An initial candidate from development head
`c305e6c455b29a63163d3604da1267643b5a1503` was rejected during exact-head
review because the published `0.3.1` source revision is
`da55c14d95ef61140d3fa9c047df333aaea8798c`. It is not accepted D2 evidence.
The corrected recovery independently archived `da55c14...`, built its wheel,
and proved that wheel byte-identical to the released `0.3.1` wheel at SHA-256
`173d9da39b891d53a9ee60cac543d049da0f58aa7402a5c6db24c93e52388fb1`.
It then rebuilt the release image from that wheel and published-revision
container assets rather than relabelling or reusing the development-head
wheel. After the Docker Hub endpoint
reset twice, read-only Buildx inspection proved that
`public.ecr.aws/docker/library/python@sha256:ac76900038d8606cc99b413d4ede77bc7152f1e42b94cf5d50d4b80a999652fe`
serves the same pinned manifest. The build used that official transport
reference without changing the Dockerfile's base digest or labels. One
incomplete upstream read was retried once after a 60-second backoff; the
release `fetch_inputs.py` then SHA-verified all five `inputs.json` artifacts.
The accepted candidate identity is immutable local image ID
`sha256:75b74528663f7b3bc06a48292c13a488447c5f32581fc461abdc242bf9321e13`,
not its convenience tag and not the public GHCR digest. Label readback records
SeqEvi revision `da55c14...`, version `0.3.1`, the official Docker Hub base name
and unchanged base digest.

D2 smoke and the separate no-Store/no-`run_batch` probe passed and removed
their exact containers. ToolRuntimeDigest is
`sha256:3e32c40479388ea1dcb4040f769a143ae28d28ee051d1932bb7d8fecfd2fd61a`.
The fixed-50 and sizing canonical EvidenceKey JSON-set digests are respectively
`79a2b436d1f1134a31c44e4c4692ae8224eb1172d9be56129d75e3743d39e486`
and `dbfb48e8e21d7254cd15de76df9c2bb47993b8712b33e71caed1fdbc0379e00c`;
all 50 and all 2,729 keys were naturally missing. Store/CAS/resource state was
unchanged, the ordinary public profile remained absent, and D2 accepted the
representative all-missing D4 branch. D4 must still run before D3.
An unrelated external terminal briefly attempted a mutable GHCR tag pull while
the correction was in progress. It is excluded from this lane's provenance and
evidence; final authoritative readback found its PID gone and the fixed public
digest still absent.
The retained public summary is the [D2 local-candidate acceptance
record](../benchmarks/20260819-v1.0-dbcan-d2-local-candidate-acceptance.md).

The fixed 50-record input is:

```text
/home/fqzhang/tmp/seqevi-next-gate-fasta/Lacticaseibacillus_rhamnosus_GG-50.fasta
SHA-256 e595adb96e198cabf30fda34adf13d25d3697df42861e60dba3c22a0192a616a
```

Use the accepted 2,729-record Lacticaseibacillus inventory identity from the
[eggNOG target-Store closeout](../benchmarks/20260818-v1.0-eggnog-target-store-closeout.md):
2,729 records, 2,729 unique keys, FASTA SHA-256
`398fb6d7d7f5a51dab7baf0ae956589f11cfe297a6ad3d5f4cf4eb865e74d582`,
and sorted key-set SHA-256
`0c08b627f2adb3e5182989423d4a67aa09038e9069ea685bf948228d8d86527a`.
Its prior retained basename is
`Lacticaseibacillus_rhamnosus_GG_uniprotkb_taxonomy_id_568703_2025_08_26.fasta`,
but D1 must read back and freeze its live absolute path and digest. The
unpublished historical path is not inferred from the accepted basename.

## Runtime Research And Sizing Rationale

The exact dbCAN 5.2.9 wheel is SHA-256
`daf39033e9921d116f46a374714f6095b71394eb6438035f1754354d7e20d8d2`.
The upstream `v5.2.9` tag resolves to
`614c93f896939042ae5bd574b9c6b971e80803f6`, the source commit frozen by the
repository's [`containers/dbcan/inputs.json`](../../containers/dbcan/inputs.json),
[`SOURCE-CORRESPONDENCE.md`](../../containers/dbcan/SOURCE-CORRESPONDENCE.md)
and enforcing [`test_dbcan_image_assets.py`](../../tests/test_dbcan_image_assets.py).
See the [dbCAN 5.2.9 release](https://pypi.org/project/dbcan/5.2.9/) and exact
[`run_dbCAN_CAZyme_annotation`
source](https://github.com/bcb-unl/run_dbcan/blob/614c93f896939042ae5bd574b9c6b971e80803f6/dbcan/core.py#L108-L165).
`614c93f...` has parent `36737a9b990645661be20a50edaadab481a95b17`;
their only repository-tree difference is `docker/environment.yml`, and their
relevant `dbcan/core.py` files are byte-identical with SHA-256
`b8351dd123022a8240e0c0915f88511ff8e8553ba4814b861df041f1d5b86e39`.
This reconciles the earlier parent-commit reference without substituting it for
the image's pinned source. The pinned source invokes DIAMOND, dbCAN-HMM and
dbCAN-sub-HMM in order, then builds the overview. The command creates all three
configurations from one `threads` option; DIAMOND and both PyHMMER
configurations therefore receive the same value. It does not execute the three
methods concurrently.

[PyHMMER's performance guide](https://pyhmmer.readthedocs.io/en/stable/guide/performance.html)
explains that target sequences are prefetched and shared across workers while
queries are distributed, but also that useful scaling depends on query and
target workload. It supports a measured gate, not an assumption of linear
64-thread scaling.

Existing [dbCAN runtime evidence](../benchmarks/20260804-v1.0-dbcan-runtime-validation.md)
proves scientific output equality at one and four threads and reports an
estimated 24.5 GB dbCAN-sub model footprint. It does not size 32 or 64 threads.
The [eggNOG full-proteome benchmark](../benchmarks/20260723-v1.0-eggnog-full-proteome-tuning.md)
found only about 12% improvement from 32 to 64 threads. That is comparison
evidence for avoiding an automatic thread sweep, not proof of dbCAN behavior.

The dated 2026-08-19 host snapshot reports 192 logical CPUs, 503 GiB RAM and
approximately 431 GiB available. It is planning evidence only. D1 must refresh
logical/physical CPU topology, total and available memory, scratch and result
filesystems, Ceph capacity/I/O state, Docker storage, Store/CAS capacity and
other active workload immediately before execution.

## Safety And Evidence Contract

Every mutating gate preserves:

- the exact released package and public kit image/profile identity. On the
  ordinary path, execution uses that acquired fixed digest and profile. On the
  approved recovery path, the public digest/profile remain the unchanged
  release identity even while absent, and the benchmark launch boundary must
  bind execution exclusively to accepted immutable local image ID
  `sha256:75b74528663f7b3bc06a48292c13a488447c5f32581fc461abdc242bf9321e13`;
- the D2-probed ToolRuntimeDigest and its published `0.3.1` source/wheel,
  release Dockerfile/locks/inputs and runtime-component lineage, plus the exact
  ResourceID, adapter contract, result schema and canonical semantic parameters;
- read-only FASTA and resource mounts, caller numeric UID/GID, no Docker socket
  mount, and no resource content, mode, owner, size or mtime change;
- one isolated project root, DuckDB output, JSON output, stderr/stdout capture
  and container name per caller;
- exact command argument arrays, start/end times, exit codes, configured
  internal OCI timeout, wall time, CPU, peak RSS, peak scratch and Ceph I/O;
- Store health and before/after sequence, evidence, artifact and coordination
  counts; PostgreSQL bytes; CAS inventory, bytes, digest reconciliation and
  temporary-residue scan;
- computed, cache-hit, hit and no-hit counts plus DuckDB metadata, native table
  schemas, row/input-map counts, semantic parameters and focused content
  checks; and
- independent later-process replay with zero external computation, followed by
  removal of every invocation-owned `seqevi-annotate-oci-*` or
  `seqevi-setup-smoke-*` container relative to the frozen pre-run baseline.
  Persistent deployment containers, including the Store and PostgreSQL, are
  excluded and must not be removed.

Write the supported setup and annotation result to JSON rather than parsing
Rich output. A concrete command skeleton is:

```bash
uvx --from seqevi==0.3.1 seqevi setup dbcan-cazyme \
  --resource /cephfs_data/genostack_v3/genostack_php/public_file_data/database/bioinfo/resources/dbcan/database/db_v5-2-9_5-5-2026/raw \
  --dry-run --json > setup-dry-run.json

uvx --from seqevi==0.3.1 seqevi setup dbcan-cazyme \
  --resource /cephfs_data/genostack_v3/genostack_php/public_file_data/database/bioinfo/resources/dbcan/database/db_v5-2-9_5-5-2026/raw \
  --yes --json > setup-apply.json

uvx --from seqevi==0.3.1 seqevi annotate \
  --profile dbcan-cazyme \
  --store http://192.168.30.205:18082 \
  --fasta <frozen-input> \
  --output <isolated-result.duckdb> \
  --threads 64 \
  --timeout-seconds <finite-preflight-value> \
  --json > <isolated-result.json>
```

The evidence wrapper records the arrays before launch and monitors resources;
it must not inject a hidden image, override the kit, use `shell=True`, or write
inside the Store artifact root.

For managed dbCAN, Docker is the OS-owned runtime boundary. Every annotation
has a finite recorded SeqEvi OCI timeout. D3, D4, replay, pilot and expansion
operators do not add the master plan's eggNOG/InterPro external host-process
watchdog. On success, failure, cancellation or timeout, cleanup targets only
the exact generated container name with `docker rm --force`, waits for that
removal to become authoritative, and verifies zero residual invocation-owned
`seqevi-annotate-oci-*` containers against the pre-run baseline. It never
cleans persistent `seqevi-store` or PostgreSQL containers. This exact-container
contract is the sole managed-dbCAN containment/deadline interpretation;
generic systemd scopes and host process-group hardening are not prerequisites
because no adapter process is launched outside that container boundary.

For this AO-owned D4 execution only, the benchmark local-candidate harness has
an additional host-containment prerequisite at Docker `create`: it reads the
authoritative `AO_SESSION_ID`, fails closed when that identity is absent or
invalid, rejects a pre-existing `ao.session` label, and adds exactly one
`ao.session=<session identity>` label to the invocation-owned container. The
reviewed prerequisite is commit
`8fd4b0053c128375dd2fda6a6f4b19ed91b88031`, harness Git blob
`7dec5ec8876167a9b8af57357197c1c4cb17ed4b` and file SHA-256
`aed07c0a9d1cc49f625c5c9ca8dcedbf7b2aae996031c4ef4e2e1f6a56aa90e1`.
D4 must read back all three after this prerequisite merges. This benchmark-only
containment label does not alter the product OCI implementation, public kit,
profile or image contract; it is not an image override, wrapper or public-pull
prerequisite.

Store mutation is forward-only. Never delete valid evidence or CAS artifacts
to manufacture cold work or make a retry look clean.

## Key-State Branches

D1 inventories the 50-record and 2,729-record EvidenceKeys under the exact
frozen identity before D3. The fixed 50 may overlap the 2,729 input, so D3 must
take one of these honest branches:

- **all missing:** run the full 50-record managed target computation, then an
  independent later-process zero-compute replay;
- **partial:** compute only the missing keys through ordinary `annotate`,
  preserve the existing-key hits, and accept the exact computed/cache split;
  do not label this cold; or
- **all present:** accept only an independent later-process zero-compute replay
  and exact zero Store/CAS growth; do not launch dbCAN merely to repeat parity.

Apply the same inventory rule before D4 and every expansion input. Capacity
projections use actual missing-key counts. All-present branches reserve only
bounded result/scratch needs and require exact zero Store/CAS growth; missing
branches use measured growth and final-reserve headroom.

## Execution Graph

```text
accepted eggNOG closeout
  -> D0 docs rebaseline
  -> D1 read-only Preflight + focused timeout-cleanup prerequisite if needed
  -> D2 supported managed setup dry-run/apply
  -> D4 one-process 64-thread 2,729 sizing + independent replay
  -> D3 fixed 50 target gate + independent replay
  -> D5 current-path unavailable disposition (no same-key gate claim)
  -> selected expansion at file concurrency 1
       -> optional measured two-file expansion
  -> dbCAN closeout
  -> InterPro Preflight under the master plan
```

### D0: Documentation Rebaseline

**Predecessor:** accepted eggNOG target-Store closeout.

Publish this plan and update only the master plan's current continuation,
target release, current graph, adapter-specific concurrency and separate
dbCAN-gate link. Update the documentation index. Do not edit accepted benchmark
evidence or its closeout addendum.

**Acceptance:** the active ordering is unambiguous, InterPro remains pending,
links resolve, statuses agree and repository validation passes. This slice
performs no annotation, Store mutation, image pull or runtime code change.

### D1: Read-Only Preflight

**Predecessor:** D0 merged; no active seeding writer.

Perform read-only released-wheel, kit, resource, image, ingress, Store, CAS,
input, key-state and capacity inspection. Specifically:

1. resolve `seqevi==0.3.1` from the released distribution with no checkout
   import, and read back the bundled kit ID and exact image digest;
2. run setup dry-run only; verify the resource lock, ResourceID, four required
   components and read-only feasibility without repeating a manual hash of the
   5.1 GiB dbCAN-sub model while its frozen lock remains valid;
3. inspect the exact digest without pulling it; if absent, record that D2 will
   acquire it through supported setup apply;
4. read the deployment-owned listener, reverse-proxy/NAT and service
   configuration to map `127.0.0.1:18081` and `192.168.30.205:18082` to the
   same exact service instance; from that service's authoritative deployment
   configuration, read back the same database target and artifact-Store root,
   then obtain a read-only database/Store identity and schema-revision
   readback. Record sanitized identities and configuration digests. Equal
   health/capability payloads are supporting evidence only and cannot replace
   this mapping. Stop if the authoritative mapping is unavailable or differs;
5. through both endpoints, verify health/capabilities and read the same frozen
   known EvidenceKey or other deployment-owned immutable Store sentinel, while
   the authoritative database/CAS path supplies the identity proof; read before
   counts, reconcile CAS rows/files, and scan coordination/temp residue without
   mutation;
6. read back the fixed 50 digest and freeze the live 2,729 path, file digest,
   record count, unique key count, sorted key-set digest, total amino-acid
   residues, minimum/maximum sequence length, `p50`/`p90`/`p95`/`p99` length
   bounds and a digest of the complete sorted length vector for later
   workload-shape comparisons;
7. independently derive ResourceID from the validated lock's ordered component
   names and SHA-256 values using the released adapter formula; record the
   derivation input and result rather than treating ResourceID as a setup JSON
   field;
8. if the exact released-image ToolRuntimeDigest is already available from an
   accepted read-only probe, inventory expected present/missing EvidenceKeys
   separately for the 50 and 2,729 sets and their overlap. If the image is
   absent or that digest is not proven, record only input SequenceIDs and defer
   exact EvidenceKey inventory to D2; do not reuse a superseded candidate's
   digest; and
9. refresh CPU, RAM, scratch, Ceph I/O/capacity, Docker, Store and CAS capacity;
   freeze a finite OCI timeout, minimum final reserves and stop thresholds.

Repository inspection on 2026-08-19 found direct success/nonzero-exit cleanup
tests in `tests/test_oci.py`, but no direct timeout case proving that a timed-out
`docker start --attach` still reaches exact-container `docker rm --force`.
Before D2, deliver one narrowly scoped focused-test PR for that behavior if a
fresh exact-head test inspection reaches the same result. Do not turn it into a
generic harness, universal runner or systemd-hardening project.

**Acceptance:** the authoritative deployment mapping proves both endpoints
reach the same service, database and artifact Store; the independently derived
ResourceID matches the fixed value; input identities are frozen; exact key
inventory is frozen only when the released-image ToolRuntimeDigest is already
proven and is otherwise explicitly deferred to D2; Store/CAS is healthy and
reconciled; capacity and timeout values are fresh and sufficient; the direct
timeout-cleanup test exists and passes. Any unexpected Store delta, identity
drift, unavailable mapping/input or insufficient headroom stops the lane.

### D2: Supported Managed Setup

**Predecessor:** accepted D1 and its focused test prerequisite.

Run the JSON dry-run again against the frozen resource. A public digest-pinned
setup apply is optional transport evidence: if selected, it may pull only the
fixed digest, run its supported ephemeral smoke and atomically publish or reuse
the ordinary managed profile. The recorded two timed-out attempts are the final
public acquisition attempts for this lane. Under the approved recovery
addendum, use the provenance-frozen release-equivalent local candidate through
the repository-owned launch-boundary harness; do not add hidden production
image injection or a profile/image override.

After public image acquisition or accepted local-candidate build, run a separate
read-only probe inside an ephemeral container created from the frozen runtime
identity. Mount the validated resource
read-only, mount no Store and invoke the released adapter's runtime-identity
path without `run_batch` or annotation. Record dbCAN and DIAMOND versions,
normalized runtime-component digests and the resulting ToolRuntimeDigest. The
probe must not infer runtime identity from the image digest. Public acquisition
uses the acquired fixed image; accepted recovery uses the freshly rebuilt,
published-revision local candidate frozen by the recovery addendum, never the
superseded development-head candidate. Remove the exact probe container and
prove that no new invocation-owned setup-smoke or probe container remains
relative to the pre-run baseline.

Then independently rederive ResourceID from the validated lock and exact kit
component order, construct the complete dbCAN EvidenceKeys with the probed
ToolRuntimeDigest and frozen semantic parameters, and repeat authoritative
present/missing inventory for both inputs before D3. Freeze the D4
representative sizing branch in advance as **all 2,729 keys naturally missing**;
any other inventory is replay/partial evidence only and cannot supply sizing
rates or expansion headroom.

**Acceptance:** dry-run and any successful apply JSON agree only on fields they
actually report, including kit, image, resource path/status and component
validation; failed apply attempts remain explicit transport evidence. The
separately derived ResourceID matches before and after execution; smoke passes;
the ordinary profile is published/equal after successful public apply or its
absence is recorded after local recovery; resource metadata is unchanged. The
public digest is inspectable when public acquisition succeeds, or the local
candidate provenance and immutable image-ID readback pass every addendum gate.
The released runtime probe supplies the frozen
ToolRuntimeDigest; repeated exact-key inventories and the D4 branch decision
are recorded; all smoke/probe containers are removed and zero residual
invocation-owned setup-smoke/probe containers remain relative to the baseline.
Persistent deployment containers are unchanged. Setup JSON is not claimed to
expose ResourceID.

### D3: Fixed 50-Record Managed Target Gate

**Disposition:** accepted on the post-D4 all-present branch. The first managed
run and independent later process each reported 50 cache hits and zero
computed, with zero Store/CAS growth. The retained
[D3 acceptance record](../benchmarks/20260819-v1.0-dbcan-d3-target-store-replay-acceptance.md)
explicitly does not claim a cold 50-record computation.

**Predecessor:** D4 disposition recorded; fresh health, identity mapping,
key-state and capacity readback. D4 runs first because the fixed 50 may overlap
the 2,729 sizing set: allowing D3 to compute a missing overlap key first would
invalidate the predeclared all-missing D4 sizing branch.

Run one managed caller at 64 threads using the branch established in D1. For an
all-missing or partial input, only ordinary ClaimSession behavior determines
which keys compute. For an all-present input, run replay only. Follow with an
independent later process using a separate output and JSON file.

When an accepted all-missing D4 run has already populated the overlapping
fixed-50 keys, D3 takes the refreshed all-present or partial branch honestly.
It does not claim a separate cold 50-record computation and does not delete or
exclude overlap to manufacture one.

**Acceptance:** both outputs are complete, schema-correct and scientifically
consistent with the accepted native contract; the first run's computed/cache
split equals the refreshed missing/present inventory; replay reports 50 cache
hits and zero computed; evidence-row growth equals exactly the missing dbCAN
EvidenceKey union. Sequence-row growth is reconciled separately to canonical
sequences absent before the run and may be zero; artifact row/file/byte growth
is batch-level and reconciles each declared raw/normalized digest rather than
the key count. The independent replay adds no evidence, sequence or artifact
rows, files or bytes. All artifacts reconcile; mounts and UID/GID are correct;
resource state is unchanged; every run-owned `seqevi-annotate-oci-*` container
is absent relative to the D1 baseline while persistent deployment containers
are unchanged. This is target execution and replay, not repeated
direct-versus-managed parity. Acceptance records that D4 was disposed first
and classifies any D4-created overlap as pre-existing for D3; it never
retroactively describes those keys as a D3 cold computation.

### D4: 2,729-Record One-Process Sizing

**Disposition:** accepted on the representative all-2,729-naturally-missing
branch with independent zero-compute replay. The retained
[D4 acceptance record](../benchmarks/20260819-v1.0-dbcan-d4-target-store-sizing-acceptance.md)
freezes the measured shape, rates, resource observations, Store/CAS growth and
cleanup.

**Predecessor:** D2 accepted, including the released runtime-identity probe,
repeated exact-key inventory and representative-branch decision; the reviewed
AO host-containment harness prerequisite merged and its exact commit/blob/file
digest read back; refreshed live path/digest/key inventory, frozen
residue/length workload and capacity gate. No target annotation has run since
that inventory, and all 2,729 EvidenceKeys remain naturally missing under the
exact released identity.

Run exactly one managed dbCAN process at 64 threads, followed by an independent
later-process replay. Do not run an 8/32/64 sweep. Measure phase-aware CPU,
peak RSS, scratch, Ceph read/write I/O, Store/CAS growth, DuckDB size and wall
time. Record enough timeline evidence to distinguish DIAMOND, dbCAN-HMM and
dbCAN-sub-HMM phases where logs permit without modifying the runtime.

If any of the 2,729 keys is present at the final pre-run readback, do not call
the result a sizing run. Take the ordinary partial or all-present replay branch,
validate it, mark dbCAN sizing rates and two-process headroom unavailable, and
prohibit selected or concurrent expansion. Never delete Store data or choose a
post-inventory substitute merely to recover a cold sizing claim.

If D3 or any other target annotation runs after D2 inventory but before D4,
the representative D4 branch is invalid even when the observed present keys
are outside the known fixed-50 overlap: D4 must take the partial/replay-only
disposition and expansion remains closed. A new readback cannot restore the
original all-missing premise.

**Acceptance:** computed/cache counts equal the pre-run inventory; native
DuckDB/JSON checks pass; replay is 2,729 cache hits and zero computation;
evidence-row growth equals exactly the pre-run missing dbCAN EvidenceKey union.
Sequence-row growth is read back separately against canonical sequences absent
before the run and may be zero. Artifact row/file/byte growth is reconciled at
batch level through every declared raw/normalized digest and its references; it
is not required to equal the key count. The independent replay adds no
evidence, sequence or artifact rows, files or bytes. No resource or container
residue remains: every run-owned `seqevi-annotate-oci-*` container is absent
relative to the D1 baseline, and persistent deployment containers are
unchanged. Only the frozen all-2,729-naturally-missing branch supplies the
dbCAN-specific rates and two-process headroom decision. A partial or replay-only
result supplies correctness evidence but no rates and leaves expansion closed.
Neither the 24.5 GB estimate nor eggNOG scaling substitutes for this
measurement. Acceptance also requires the retained timeline to prove D4 ran
before D3 and before every other target annotation after the D2 inventory.

### D5: Same-Key Managed Gate Disposition

**Disposition:** unavailable on released SeqEvi 0.3.1. Exact release-source
readback confirms Docker create/start precedes the inner ClaimSession open, so
no same-key process was launched. The retained
[closeout](../benchmarks/20260819-v1.0-dbcan-target-store-closeout.md) records
the source blobs and ordering evidence.

**Predecessor:** D4 disposition recorded; exact SeqEvi 0.3.1 managed dispatch
read back from the released source.

Current managed-v2 dispatch starts the outer OCI container before the inner
SeqEvi process opens ClaimSession. There is no claim-before-OCI mechanism that
could prove a waiter launched no container. D5 is therefore **unavailable** on
the fixed release and no two-process run is launched or represented as a
same-key managed gate. Proof that one already-started waiter container skipped
its inner dbCAN batch would be a different, weaker claim and is not accepted as
a substitute.

**Acceptance:** record the released source paths and exact commit that establish
the dispatch order, record D5 as unavailable because the product path lacks the
required boundary, and make no Store mutation. Enabling D5 requires a separate
authorized runtime contract/code change that moves claim acquisition before
OCI dispatch, with focused tests and a new reviewed execution disposition.

### Selected Expansion

**Predecessor:** D4 accepted specifically on the frozen all-2,729-naturally-
missing sizing branch; D5 recorded unavailable under current managed-v2
dispatch; exact selected manifest and missing-key inventory frozen. A partial
or replay-only D4 closes expansion rather than satisfying this predecessor.

Start at file concurrency one. For each file, repeat fresh identity, health,
capacity and key-state checks; run one 64-thread managed process; validate
DuckDB/JSON and Store/CAS; then run an independent zero-compute replay.
Selection and progression are reviewed from measured record counts and D4
rates, never from an unrecorded directory glob.

For each selected run, evidence-row growth must equal exactly that run's
predeclared missing dbCAN EvidenceKey union. Sequence-row growth is read back
separately under the new-sequence/already-present-sequence rules and may be
zero. Artifact row/file/byte growth is reconciled at batch level by declared
raw/normalized digests and their references, never as one artifact per key.
The independent replay must add none of those rows, files or bytes. Each run
must also remove its exact `seqevi-annotate-oci-*` container and leave no new
invocation-owned annotate/setup-smoke container relative to the D1 baseline;
persistent deployment containers remain unchanged.

D4 measures one dbCAN external invocation below SeqEvi's 10,000-identity
adapter batching boundary. Therefore this plan permits expansion only within
that exact measured execution shape: each selected file must contain at most
2,729 unique canonical keys in total and at most 2,729 missing dbCAN
EvidenceKeys at final readback, so it produces no more than one external dbCAN
batch. Its sequence workload must also be no greater than the D1-frozen D4
shape: total residues, maximum length and every frozen
`p50`/`p90`/`p95`/`p99` length bound must each be less than or equal to D4,
with its complete sorted-length vector digest retained as evidence. A file that
exceeds any key, residue or length bound remains closed,
including typical larger corpus files. It requires a separately reviewed
naturally missing representative sizing input for that workload; a gate above
the key cap must cross the batching boundary and measure repeated-invocation
CPU, RSS, scratch, Ceph I/O, Store/CAS and result growth before any expansion
claim.

A two-file expansion after sizing is optional:

- before either pilot, freeze two inputs whose complete dbCAN EvidenceKey sets
  are naturally all missing and mutually disjoint at authoritative final
  readback. Each input must independently satisfy every D4 total/missing-key,
  residue, length-distribution and single-batch cap. Retain each input and
  EvidenceKey-set digest, their empty intersection proof, the D4 post-run
  present-key inventory, and proof that neither pilot set was seeded by D4 or
  any intervening serial expansion. Never delete Store data to create this
  state. If any premise cannot be proven, do not run or use that pilot to
  authorize its concurrency shape;
- start the two-at-32 pilot with one such frozen pair, and require both callers
  to compute their complete disjoint missing sets; and
- if that passes, run one separately bounded two-at-64 pilot before authorizing
  any further two-at-64 execution. It must use a different independently frozen
  cold/disjoint pair because the two-at-32 pair is now present. The pilot itself
  is the measurement gate: both complete batches must run, and combined CPU,
  peak RSS, scratch, Ceph I/O, Store/CAS health and complete two-process
  headroom must pass with the D1-frozen final reserves intact.

The optional gate proves different-file process concurrency only. It does not
claim same-key managed coordination and does not change the default file
concurrency of one. A failed two-at-32 gate returns execution to one file. A
failed two-at-64 pilot closes all further two-at-64 expansion and returns to
the last accepted shape, without deleting accepted Store state. A cache hit,
overlap, incomplete batch or unprovable D4-to-pilot key-state lineage invalidates
that pilot's authorization even when its resource measurements look healthy.

## Stop, Cleanup And Recovery

Stop and preserve evidence on any package, image, kit, ResourceID,
ToolRuntimeDigest, semantic-parameter, input or schema drift; timeout; Store
health/commit error; unexplained row or byte delta; failed artifact
reconciliation; output defect; resource write; insufficient headroom; or
new invocation-owned container residue relative to the D1 baseline. On timeout
or cancellation, force-remove only the exact owned container, verify its
absence and then scan only the bounded `seqevi-annotate-oci-*` and
`seqevi-setup-smoke-*` namespaces against that baseline. Never remove or count
`seqevi-store`, `seqevi-postgres-fuq11` or other persistent deployment
containers as invocation residue. An uncertain external write is read back and
deduplicated before any retry.

Retry only after authoritative Store, CAS, output and container readback shows
the intended state. Preserve valid partial progress and use the partial or
all-present branch on the next reviewed attempt. There is no destructive
rollback of immutable Store data.

## Exclusions

The original D0 documentation review unit excluded:

- repeated direct-versus-managed dbCAN parity;
- full per-adapter C1/C2 reruns;
- repeated manual hashing of the 5.1 GiB model while its frozen lock is valid;
- hidden production image injection, public CLI/profile image override or
  private candidate profile substitution; the explicit benchmark-only local-ID
  launch-boundary harness is the sole approved recovery exception;
- a universal runner, generic harness project, systemd hardening or host
  process-group redesign;
- runtime code, Store schema, adapter or evidence-contract changes;
- InterPro execution or hardening; and
- annotation or Store mutation during the D0 documentation review unit.

## Closeout And Review Units

The D0 documentation rebaseline closed Issue #55 after the plan, master
continuation and documentation index agreed and passed validation. The narrow
D1 Docker attach-timeout cleanup prerequisite is merged; D1 read-only
Preflight, the corrected published-revision D2 recovery, representative D4
sizing/replay and all-present D3 replay are accepted. D4 ran before D3, and D3
is explicitly cache reuse rather than a cold 50-record computation. Their
retained [D4](../benchmarks/20260819-v1.0-dbcan-d4-target-store-sizing-acceptance.md)
and [D3](../benchmarks/20260819-v1.0-dbcan-d3-target-store-replay-acceptance.md)
records state the actual annotation, Store/CAS and cleanup boundaries.

The dbCAN execution units are closed as follows:

1. D5 is explicitly unavailable on current managed-v2 dispatch; no same-key
   process was launched;
2. selected expansion and two-file pilots were not selected or authorized and
   remain future separately gated work; and
3. the [dbCAN closeout](../benchmarks/20260819-v1.0-dbcan-target-store-closeout.md)
   hands the active continuation to InterPro read-only Preflight.

Final dbCAN closeout requires accepted D1-D2; a retained D2 inventory followed
by D4 disposition before D3; D4 either accepted on its frozen representative
all-missing branch or explicitly recorded replay/partial-only with sizing and
expansion unavailable; accepted D3 under its refreshed post-D4 key state; D5
explicitly recorded unavailable on current managed-v2 dispatch without a
same-key claim; every permitted selected expansion and replay accepted; final
Store/CAS/result/capacity reconciliation that separately accounts for exact
missing-union evidence-row deltas, possibly-zero new-sequence deltas and
batch-digest artifact rows/references/files/bytes; zero residual
invocation-owned containers relative to the frozen baseline and unchanged
persistent deployment containers; retained D4-post-run and final pre-pilot
inventories, cold/disjoint pair-set digests and empty-intersection proofs for
every concurrency shape claimed; and an explicit statement of unseeded inputs.
Any D3-before-D4 execution forces
the replay/partial-only D4 closeout branch. Files above the D4-measured
single-batch shape are recorded as unseeded, not silently admitted to
expansion; the same applies to files exceeding the frozen D4 residue or
sequence-length workload caps. A failed two-at-64 pilot records that expansion
shape closed. Closeout does not by itself close global cache seeding.

The original D0 documentation PR ran repository-local link/status consistency
checks plus the canonical `pdm run check` and closed Issue #55 without
annotation, Store mutation, image pull or runtime code change. The later D1-D2
review units explicitly include the focused OCI test, benchmark-only local-ID
launch boundary, recorded setup transport attempts, local image build,
ephemeral smoke, runtime-identity probe and read-only inventory/readback. They
still performed no annotation or Store/CAS mutation. Each successor review unit
must state its actual code and operational evidence boundary rather than reuse
the D0-only declaration.

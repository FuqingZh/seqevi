# Shared Store Cache-Seeding Implementation Plan

Version: v1.0
Date: 2026-08-07
Status: approved execution plan; dbCAN target-Store Preflight pending
Target release: SeqEvi 0.3.1
Original issue: #22
Current continuation: Issue #55

> **Continuation authority:** for eggNOG target-Store closeout, continue with
> the approved [eggNOG target-Store closeout
> addendum](20260817-v1.0-eggnog-target-store-closeout-implementation-plan.md).
> That successor controls only the eggNOG release, deployment, evidence-reuse,
> and target-Store closeout deltas it states. All other gates in this plan
> remain in force.
> The addendum's [accepted 2026-08-18 benchmark
> record](../benchmarks/20260818-v1.0-eggnog-target-store-closeout.md) closes
> the eggNOG lane only. The approved [dbCAN target-Store execution
> plan](20260819-v1.0-dbcan-target-store-execution-plan.md) supersedes only the
> active next-lane ordering and the generic dbCAN gate below: dbCAN Preflight
> now precedes InterPro. Global cache seeding remains incomplete.

## Goal

Safely seed the existing PostgreSQL and POSIX-artifact shared Store from the
measured FASTA corpus with released `seqevi==0.3.1`. Execution advances through
small, auditable scale gates, preserves adapter-native schemas in independent
DuckDB results, and records machine-readable JSON summaries. The plan first
proves concurrent Store behavior, then expands only while identity, capacity,
health, output, and replay gates remain satisfied.

This remains an approved execution plan. Progress claims below are limited to
the linked accepted evidence; unlisted gates have not passed.

## Current Progress And Continuation

The accepted [ClaimSession cancellation and contention
record](../benchmarks/20260814-v1.0-claim-session-cancellation-acceptance.md)
closes ClaimSession and cancellation hardening, including a 64-thread
candidate-source eggNOG contention run with independent zero-compute replay
against a fresh Store. That run remains separate mechanism evidence and is not
target-Store execution. The later target-Store acceptance is recorded only by
the eggNOG closeout record below.

The [eggNOG target-Store closeout
record](../benchmarks/20260818-v1.0-eggnog-target-store-closeout.md) now accepts
that lane using the released 0.3.1 service/client and the all-present frozen-set
branch. It keeps the accepted fresh-Store C2 mechanism run distinct from the
target Store replay. The dated [dbCAN execution
plan](20260819-v1.0-dbcan-target-store-execution-plan.md) now controls the
active continuation through dbCAN D0-D5 and selected expansion. InterPro
Preflight resumes only after dbCAN closeout. No dbCAN or InterPro target
execution is accepted by the eggNOG record. Global cache seeding remains
incomplete.

The applicable backup/export boundary is:

- For the current test/cache-seeding Store, preserve immutable Store state and
  treat the prior export/readback validation as a historical precondition. No
  fresh coordinated production backup is required, and this plan records no
  export identity that is not already evidenced.
- If the Store becomes production authority, each gate below that invokes this
  boundary requires a fresh coordinated PostgreSQL/artifact backup and
  successful completion/identity readback before mutation; failure stops the
  gate.

Both cases retain fresh health, identity, input, output-isolation, and capacity
checks before the resumed Preflight and each later adapter lane.

For a fully all-present frozen lane, capacity acceptance is exact zero
PostgreSQL/CAS growth plus measured retained-result bytes and sufficient
bounded project/output-root availability. It does not require a fictional
cold-growth reservation. Any missing key returns the lane to the ordinary
measured-growth and headroom gates below.

Before the later InterPro Preflight, the acceptance harness must add and test a
per-adapter timeout, a longer external containment watchdog, and complete
process-group termination and descendant-reap fencing. Normal-exit process
scans do not establish those failure paths. They are not prerequisites for
managed dbCAN, whose OS-owned runtime boundary is Docker; the dbCAN D1
prerequisite is limited to direct coverage that attach timeout still forces
removal of the exact container and leaves no residual SeqEvi container.

## Non-Goals

- changing a product API, Store schema, evidence identity, or adapter contract;
- creating a universal annotation table or merging eggNOG and Pfam schemas;
- writing Store database rows or artifact files by hand;
- immediately running every file or every adapter;
- downloading, copying, redistributing, or taking ownership of caller-managed
  annotation resources;
- coupling SeqEvi to a FASTA source system, project model, workflow scheduler,
  container engine, or host filesystem layout; or
- building a resumable seeding runner in the first execution.

## Fixed Execution Contract

| Boundary | Fixed value |
| --- | --- |
| client | PyPI `seqevi==0.3.1`, invoked through `uvx` |
| Store | `http://127.0.0.1:18081` |
| eggNOG profile | `eggnog-5.0.2` |
| InterPro/Pfam profile | `interpro-pfam-38.1` |
| managed dbCAN active lane | one file/process at 64 threads; optional different-file expansion starts two at 32 only after dbCAN sizing |
| resources | caller-owned, identity-frozen, mounted read-only |
| result ownership | isolated project root and DuckDB output per process/file |
| eggNOG/InterPro file concurrency | at most 2 under their existing lanes |
| eggNOG/InterPro adapter concurrency | 8 threads per process, 16 total |
| InterPro fallback | 4 threads per process when memory or I/O requires it |
| contention scope | one adapter family at a time |
| pilot root | `/home/fqzhang/tmp/seqevi-seed-<run-id>` |
| process deadline | finite adapter timeout plus external containment watchdog, frozen in Preflight |

No annotation starts unless its recorded argument array is wrapped by the
active adapter family's frozen watchdog and supplies a finite
`--timeout-seconds` no longer than that watchdog. The internal timeout owns
adapter process-group cleanup; the external watchdog owns a dedicated OS
containment boundary and cannot claim termination until that boundary is empty.

Host paths are execution parameters, not SeqEvi product contracts. Accepted
run summaries move to a `seed-runs` directory that is a sibling of the Store's
`artifacts` directory. Execution never writes summaries or other manual files
inside `artifacts`.

## Measured Starting State

### Input snapshot

The planning snapshot contains 50 FASTA files and 2,367,598 records. Parsing
each record through SeqEvi's released canonical sequence rules produced
2,102,183 unique canonical sequences and 265,415 duplicate occurrences
(`total records - unique canonical sequences`, 11.21% of records). The scan
found no invalid sequences, empty InputIDs, or duplicate InputIDs.

That planning measurement did not retain a dated per-file path/SHA-256
manifest, so matching its aggregate counts cannot prove byte-for-byte corpus
continuity. Preflight must not describe its first manifest as a successful
digest refresh against a nonexistent baseline. Instead, it creates a candidate
execution manifest, requires explicit review against the intended source
snapshot and aggregate measurements, and freezes the approved candidate as the
execution baseline before any mutating slice. If the intended snapshot cannot
be identified and approved, execution stops.

The measurement method is: enumerate the bounded corpus file list; parse every
FASTA record; validate a non-empty, corpus-unique InputID; canonicalize and
validate the sequence with the released SeqEvi identity implementation; count
records; count distinct resulting SequenceIDs across all files; and derive
duplicate occurrences from the difference. Before execution, repeat that scan
and create a sorted manifest containing relative path, byte size, record count,
unique SequenceID count, a digest of the sorted per-file SequenceID set, and
streaming file SHA-256 for every file. Retain the sorted SequenceID sets for
inputs whose overlap is an execution gate. The original planning method used
`seqevi==0.2.0`; resumed execution records the active successor
`seqevi==0.3.1`, its measurement command or script identity, and any resulting
identity comparison with the historical planning aggregate. For the initial
Preflight scan, any aggregate or validation mismatch stops approval; aggregate
agreement alone is not a continuity claim. After the execution manifest is
approved and frozen, any later count, validation result, membership, or digest
difference is input drift and stops the run pending review.

### Live Store baseline

The following cheap facts were read back on 2026-08-07 from the controller's
healthy loopback service and its PostgreSQL database. They are dated execution
evidence, not timeless architecture or an estimate of available capacity.

| Measure | Baseline |
| --- | ---: |
| `/health` | HTTP 200, status `ok` |
| sequence rows | 15,155 |
| evidence rows | 15,156 |
| artifact rows | 19 |
| artifact logical bytes | 731,362 |
| PostgreSQL database bytes | 27,449,023 |
| real eggNOG evidence | 2 |
| real InterPro-Pfam evidence | 2 |
| real dbCAN evidence | 0 |

Every execution slice takes a fresh before/after baseline using the same
queries and records Store health. Deltas, rather than absolute counts copied
from this plan, are the acceptance evidence.

## Existing Safety Invariants

No code change is required for the first gates. On `origin/main` at
`930c0ae5638d9b700b3f5f36ad098b183ff1caf6` (the 0.2.0 release), PostgreSQL
uses a composite evidence primary key over SequenceID, adapter contract
version, ToolRuntimeDigest, ResourceID, and semantic-parameters hash. Artifact
digests are primary keys. Inserts use `ON CONFLICT DO NOTHING`; a losing writer
then reads the stored row and verifies immutable semantic parameters, status,
and payload digest. A different payload for the same key fails with an
integrity conflict. It is never last-writer-wins.

The POSIX content-addressed store streams into a same-directory temporary file,
checks expected byte size and SHA-256, flushes and `fsync`s the file, and
publishes with an atomic hard link. A concurrent publisher verifies the already
published digest. The containing directory is `fsync`ed after publication, and
temporary files are removed in cleanup. Artifact uploads precede the evidence
commit, however, and equal scientific payloads may have different valid
artifact encodings. A losing writer can therefore leave a valid, unreferenced
CAS artifact. Accepted contention must reconcile evidence-referenced artifacts
separately from all uploaded artifact rows/files, account for both categories
in capacity, require one path for each individual digest, and find no
`.seqevi-upload-*` or `.seqevi-artifact-*` temporary residue. It must not claim
one artifact digest per scientific payload.

These invariants explain the expected outcome; the concurrency gate must still
exercise and measure the deployed PostgreSQL/artifact Store rather than infer a
pass from source inspection.

## Evidence Contract

Each process writes only beneath its own project root. Preserve, for every
slice:

- refreshed input-manifest identity and selected file digests;
- exact package version, command argument array, start/end time, exit code,
  wall time, peak RSS, CPU and I/O observations, configured adapter timeout,
  and external-watchdog deadline/outcome;
- adapter, ResourceID, ToolRuntimeDigest, adapter contract version, and
  canonical semantic parameters;
- computed/cache/no-hit counts and any HTTP status or retry observation;
- Store health plus before/after sequence, evidence, artifact, artifact-byte,
  and PostgreSQL-byte values;
- project-root available bytes, cumulative retained-result bytes, per-process
  peak scratch bytes, and before/after project-root usage;
- DuckDB file digest, schema identifiers, tables, row counts, input-map counts,
  and focused content checks;
- evidence-referenced artifact digests, all newly uploaded artifact digests,
  valid unreferenced losing-writer uploads, their respective byte totals,
  per-digest row/file reconciliation, and temporary-residue scan; and
- a JSON summary with the same acceptance decision and stop reason as the
  human-readable run summary.

Evidence is first written beneath
`/home/fqzhang/tmp/seqevi-seed-<run-id>`. After a gate is accepted, copy only
its sanitized manifest, summary, and small audit outputs to the Store sibling
`seed-runs/<run-id>/`. Retain every accepted DuckDB result in its declared
project root through final reconciliation; only after final completion may the
operator's longer-term retention policy remove or archive it. Never record
credentials, raw environment dumps, or unredacted service configuration.

Future durable scale and performance claims belong in a versioned document
under `docs/benchmarks/`; an execution that changes a reusable operational
procedure must update or add the appropriate document under `docs/operations/`.
Those later records contain measured results and exact environment evidence;
this plan does not invent them.

## Preflight Slice

**Predecessors:** released `seqevi==0.3.1`; access to the corpus, named
profiles, read-only resources, Store, the applicable backup/export boundary,
PostgreSQL readback, and artifact filesystem; no other seeding writer.

**Invariants and local non-goals:** this slice is read-only with respect to the
Store and performs no annotation or manual Store write; do not refresh a
profile or resource identity silently; do not infer capacity sufficiency from
filesystem free space alone.

**Actions and outputs:**

1. Create the candidate SHA-256 execution manifest with the measurement method
   above. Review its exact paths and digests against the intended source
   snapshot, reconcile the planning aggregates, record the approval, and freeze
   it before any mutating slice. Stop if source provenance or approval is
   unavailable. Every later slice refreshes against this frozen baseline.
2. Run `uvx --from seqevi==0.3.1 seqevi --version` and a harmless prewarm so
   package acquisition is outside timed gates; retain resolver/version output.
3. Validate `eggnog-5.0.2` and `interpro-pfam-38.1`; record ResourceID,
   ToolRuntimeDigest, semantic defaults, and read-only mount evidence.
4. Read Store health and baseline counts. Apply the backup/export boundary
   above. For the current test/cache-seeding Store, record the prior
   export/readback validation only as a historical precondition and do not
   invent an export identity or trigger a fresh production backup. If the Store
   is production authority, trigger the supported coordinated
   PostgreSQL/artifact backup, read back its completion state and identities,
   and retain that reference; do not advance on trigger success alone.
5. Read current PostgreSQL, artifact-filesystem, and project-root filesystem
   capacity through supported host tools and record the available-byte
   baselines. The project-root baseline covers
   `/home/fqzhang/tmp/seqevi-seed-<run-id>` and therefore retained DuckDB
   results and adapter/materialization scratch space. Treat these values as
   availability inputs only; the serial sanity slice supplies measured growth
   before any 2x projection is accepted. Freeze an operator-approved minimum
   final reserve, expressed separately in PostgreSQL, artifact-filesystem, and
   project-root bytes, before the first mutating slice.
6. Recompute and retain the sorted SequenceID sets for
   `Trichoderma_reesei_BLF.fasta` and its UniProt FASTA, prove the 9,115 set is
   a subset of the 9,116 set and their union has 9,116 members, and query the
   frozen eggNOG and InterPro-Pfam identities to prove all of those EvidenceKeys
   are missing. Reserve those exact adapter-specific EvidenceKeys for C2; every
   mutating slice before that adapter's C2 must exclude them.
7. Select and freeze the exact two C1 input manifest subsets before serial
   sizing. Retain their relative paths, file SHA-256 values, record and unique
   SequenceID counts, sorted SequenceID-set digests, disjointness/union
   evidence, and adapter-specific missing-key upper bounds. Both sets must be
   disjoint from each other, the Lacticaseibacillus sanity SequenceIDs, and the
   active adapter's reserved C2 keys. Query both frozen adapter identities to
   prove every C1 EvidenceKey is missing, then reserve those exact keys; the
   sanity slice must not seed them.
8. Freeze a finite internal `--timeout-seconds` and a longer finite external
   watchdog deadline separately for eggNOG and InterPro-Pfam from accepted
   runtime evidence and the host operating bound. Never rely on an omitted or
   `None` CLI timeout. Launch each annotation in a dedicated OS-owned
   containment boundary. Internal expiry must let SeqEvi terminate the adapter
   process group; external expiry terminates the complete containment boundary,
   waits at most 60 seconds, force-terminates remaining descendants, verifies
   the boundary is empty, and enters the preservation/stop path.
9. Create independent `project-a` and `project-b` roots, result paths, logs,
   and JSON-summary paths. Verify neither output path aliases the other or the
   Store artifact root.

**Focused verification and acceptance:** the candidate execution manifest is
approved and frozen without claiming comparison to an absent per-file planning
baseline; health is `ok`; the applicable backup/export boundary is satisfied
(the historical precondition for the current test/cache-seeding Store, or
complete identity-bearing backup readback for a production authority);
profiles resolve to frozen identities; capacity availability is recorded
without a growth claim; the minimum final reserve is frozen; the C2 sets,
relationship, missing state, and reservation are retained; exact C1 inputs and
upper bounds are frozen; finite internal timeouts and containment watchdogs are
frozen; output roots are independent.

**Stop, cleanup, and rollback:** stop on any drift, failure to satisfy the
applicable backup/export boundary, identity mismatch, invalid profile/resource,
output alias, or inadequate capacity availability. A production authority
therefore stops on failed fresh backup readback; the current test/cache-seeding
Store does not require that fresh backup. Preserve evidence; never delete Store
rows or artifacts to simulate rollback. Because this slice is read-only, any
unexpected Store delta is itself a stop condition.

## Serial Sanity And Sizing Slice

The current execution graph is fixed. The eggNOG lane is accepted; the dated
dbCAN plan controls its own internal D0-D5 graph and expansion gates:

```text
accepted eggNOG closeout
  -> dbCAN D0 -> D1 -> D2 -> D3 -> D4 -> D5 -> selected expansion
  -> dbCAN closeout
  -> InterPro-Pfam sanity -> InterPro-Pfam C1 -> InterPro-Pfam C2
  -> E1 -> E2 -> E3 -> E4 -> E5 -> E6
```

Run this slice first for eggNOG. After eggNOG sanity, C1, and C2 all pass, run
it for InterPro-Pfam before that adapter's C1 and C2. This preserves one adapter
family at a time and keeps the complete eggNOG concurrency lane ahead of
InterPro-Pfam.

Use the 2,729-record Lacticaseibacillus input for one serial cold run followed
by an independent replay. The input must be disjoint from the C2 reserved
SequenceID set and both C1 reserved SequenceID sets for the active adapter
identity.

**Predecessors:** accepted read-only Preflight; satisfied applicable
backup/export boundary and fresh Store-health readback; frozen
profile/resource/semantic identity; verified C2 reservation; authoritative
lookup confirms every manifest-derived sanity EvidenceKey is missing for the
active adapter identity; no other seeding writer.

**Invariants and local non-goals:** one process and one adapter family; no
concurrency claim; no C1 or C2 reserved EvidenceKey; isolated cold and replay
project roots; no manual Store write.

**Outputs:** cold and replay DuckDB/JSON results; selected input-set identity;
computed/cache/no-hit counts; full Store before/after/replay baselines;
referenced and unreferenced artifact reconciliation; latency and resource use;
project-root retained-result use and peak scratch bytes per 1,000 input
records;
bytes-per-1,000-unique-EvidenceKeys calculation; next-slice projection.

**Focused verification:** the cold run exits zero and produces a complete,
schema-correct result; Store evidence growth equals the cold run's missing-key
set; the independent replay reports one cache hit for every manifest-derived
unique EvidenceKey and zero computed records and has a complete output.
Calculate PostgreSQL growth and total artifact growth, including valid
unreferenced uploads, plus retained DuckDB-result growth per 1,000 newly stored
unique EvidenceKeys. Record peak scratch bytes for the cold and replay
processes and derive the conservative peak scratch rate per 1,000 input
records. For the frozen C1 inputs, project cumulative retained outputs plus the
sum of the two concurrent processes' scaled peak scratch requirements,
independently from PostgreSQL and CAS growth. Use the frozen missing-key and
record-count upper bounds to require at least 2x projected headroom in all
three capacity domains. If sanity scale does not support a defensible scratch
projection, stop rather than extrapolate an unmeasured bound.

**Acceptance:** outputs and identities reconcile; replay is zero-compute; the
artifact audit reconciles every new row/file and reference; no upload temp
residue exists; measured 2x PostgreSQL, artifact, and project-root headroom is
sufficient for the exact frozen C1 inputs. Acceptance for one adapter does not
supply a growth rate or capacity decision for another.

**Stop, cleanup, and rollback:** stop on any global condition, any C2
reservation overlap, or inability to establish measured 2x headroom. Preserve
both DuckDB outputs and immutable Store state. Refresh the baseline before any
reviewed retry; do not delete valid cache entries or unreferenced CAS uploads.

## Concurrency Qualification

Concurrency qualification runs eggNOG first. InterPro-Pfam begins only after
all eggNOG concurrency and replay criteria pass. Use 8 adapter threads per
process initially; use the declared 4-thread InterPro fallback only when the
reason and resource evidence are recorded. Run one adapter family at a time.

### Slice C1: Disjoint concurrent writes

**Predecessors:** accepted serial sanity and measured 2x headroom for the same
adapter family; authoritative lookup confirms every Preflight-reserved C1
EvidenceKey remains missing. For InterPro-Pfam, eggNOG C2 must also be
accepted.

**Invariants and local non-goals:** two independent processes, projects, and
outputs using exactly the Preflight-frozen C1 manifest subsets and set digests;
disjoint input SequenceIDs that exclude all C2 reserved keys; identical
adapter/profile/resource and semantic parameters; no attempt yet to prove
same-key contention.

**Outputs:** two DuckDB files, two process JSON summaries, synchronized Store
before/after evidence, resource-use capture, artifact audit, and one combined
gate summary.

**Focused verification:** both processes exit zero; no HTTP 409/422/5xx,
deadlock, commit error, identity drift, or schema/input-map mismatch; Store
evidence delta equals the union of missing EvidenceKeys rather than an
unexplained count; both DuckDB results are complete and isolated; committed
payloads are immutable; all new artifact rows/files and evidence references
reconcile, valid unreferenced uploads are counted, each digest has one CAS
file, and no upload temporary file remains. Calculate C1's observed
PostgreSQL, total-artifact, retained-result, and peak-scratch rates. For each
C2 projection dimension, use the maximum applicable measured rate from serial
sanity and C1, not the sanity rate alone, against C2's frozen missing-key and
record upper bounds. Require at least 2x remaining PostgreSQL and artifact
headroom. Project cumulative retained C1/C2 DuckDB outputs plus C2's
two-process peak scratch requirement and require at least 2x remaining
project-root headroom. If neither measurement defensibly bounds Trichoderma's
expected native-hit/artifact shape, stop until an accepted worst-case bound is
available; do not run a C2 identity as a sizing pilot.

**Acceptance:** every check passes for eggNOG, then independently for
InterPro-Pfam. Record latency, throughput, peak resource use, computed/cache
counts, and Store deltas without claiming overlapping-write coverage.

**Stop, cleanup, and rollback:** stop the adapter family on either nonzero
exit, health/commit failure, conflict, deadlock, residue, or output defect.
Preserve both projects and Store state. Do not automatically retry a
non-idempotent external-tool run or delete accepted immutable cache entries.

### Slice C2: Overlapping same-key writes

**Predecessors:** C1 accepted for the same adapter family; satisfied applicable
backup/export boundary plus fresh health, identity, input, and measured
headroom checks; authoritative readback confirms every reserved C2 EvidenceKey
remains missing.

Use `Trichoderma_reesei_BLF.fasta` (9,116 unique sequences) for `project-a`
and its UniProt FASTA (9,115 unique sequences, a subset) for `project-b`. The
expected union is 9,116 unique SequenceIDs. Before launch, verify those
cardinalities and the subset/union relationship by recomputing the retained
sorted SequenceID sets and comparing their set digests with Preflight.

**Invariants and local non-goals:** both processes use the same
adapter/profile/resource/Store and semantic parameters. Hold both processes at
a launch barrier after Store lookup preparation where the supported client
surface permits, then release them together so both can observe misses and
compete on identical EvidenceKeys. Do not use private test injection or manual
Store writes to manufacture contention.

**Outputs:** both complete DuckDB results and JSON summaries; barrier timing;
per-process computed/cache counts; request/error observations; Store deltas;
payload equality and artifact reconciliation audit; residue scan; combined
contention verdict.

**Focused verification:**

- both processes exit zero, with no 409/422/5xx, deadlock, or commit failure;
- Store evidence growth equals the missing-key union, never the two-job sum;
- every overlapping EvidenceKey resolves to one immutable equal payload;
- every evidence artifact reference resolves; all uploaded rows/files,
  including valid unreferenced losing-writer encodings, reconcile and count
  toward capacity; every individual digest has one CAS path; no upload
  temporary residue remains;
- project outputs are complete, separate, schema-correct, and have the expected
  input-map cardinalities; and
- independent later processes replay the two inputs with respectively 9,116
  and 9,115 cache hits and zero computed records.

If one initial process sees cache entries before it computes overlapping keys,
record visibility/race coverage but do not claim same-key commit contention was
exercised. Preserve the visibility-only result and stop qualification; do not
delete immutable rows or substitute a different input pair for the frozen C2
contract.

**Acceptance:** eggNOG passes the complete gate, including independent replay,
before the same gate starts for InterPro-Pfam. InterPro-Pfam must meet the same
criteria; a documented reduction to 4 threads per process changes only
execution concurrency, not evidence identity or acceptance. After both adapter
lanes pass, apply their measured rates to E1's missing-key upper bound and
record-count upper bound. Include all retained results and E1 peak scratch, and
require at least 2x headroom in all three capacity domains before accepting
concurrency qualification.

**Stop, cleanup, and rollback:** stop on replay computed count greater than
zero, identity drift, schema/input-map mismatch, health/commit error, deadlock,
unexplained conflict or delta, payload inequality, artifact residue, or lost
headroom. Preserve all evidence and immutable Store state; do not advance
automatically.

## Expansion Slices

After concurrency qualification, ordinary expansion remains limited to two
files and one adapter family at a time. Each slice repeats the applicable
backup/export boundary plus fresh health, identity, manifest, and measured
2x-headroom gates. eggNOG completes and replays the slice before InterPro-Pfam
starts it.

| Slice | Input scope | Predecessor | Scale gate |
| --- | --- | --- | ---: |
| E1 | accepted Lacticaseibacillus sanity, E. coli reference, and `Trichoderma_reesei_BLF.fasta`; exclude the C2 UniProt subset FASTA from this selected scope | sanity and C2 accepted for both adapters | 16,247 selected cumulative records |
| E2 | E1 plus Lactobacillus, Pseudomonas, Streptococcus, and Candida | E1 accepted | 101,657 cumulative records |
| E3 | remaining microbial, fungal, and algal inputs | E2 accepted | manifest-derived |
| E4 | UP reference proteomes | E3 accepted | manifest-derived |
| E5 | taxonomy aggregates | E4 accepted | manifest-derived |
| E6 | largest high-overlap inputs | E5 accepted | manifest-derived; last |

For each expansion slice:

**Invariants and local non-goals:** select exact files by refreshed manifest,
never by an unrecorded directory glob; schedule no more than two files; keep
projects and DuckDB outputs isolated; run one adapter family at a time; make no
schema/API change and no manual Store write.

**Outputs:** selected manifest subset; commands and identities; per-file
DuckDB/JSON results; computed/cache/no-hit counts; schema, table, row, and
input-map checks; latency and resource use; Store health and deltas; artifact
audit; retained-result and peak-scratch project-root use; replay results; slice
acceptance summary.

**Focused verification:** both active processes exit zero; output schemas and
input maps match the adapter contract and selected manifests; Store evidence
delta equals the missing-key union; payload and CAS invariants hold; no temp
residue exists; independent replay has all selected unique keys cached and zero
computed. When a successor exists, measured post-slice capacity must retain 2x
headroom for that successor in PostgreSQL, artifact, and project-root domains;
the project-root projection includes every retained accepted DuckDB result plus
the successor's peak concurrent scratch requirement. For final Slice E6,
record remaining measured capacity in all three domains and require the minimum
final reserves frozen in Preflight instead of inventing a next-slice
projection.

**Acceptance:** all checks pass for eggNOG and then InterPro-Pfam, evidence is
preserved at the declared destination. For E1-E5, the next slice's exact file
list and capacity projection are reviewed; E6 instead accepts the recorded
Preflight-frozen final reserve. Cumulative record counts are selection gates,
not expected evidence-row deltas because canonical duplicates and pre-existing
cache entries are excluded from missing-key unions.

E1's 16,247 count covers exactly its three selected inputs. The C2 UniProt
Trichoderma FASTA remains part of accepted cache/evidence coverage, but because
its 9,115 unique sequences are a subset of the selected BLF input, it is not an
E1 selected file and its records are not added to the 16,247 record gate. The
refreshed manifest record counts for Lacticaseibacillus, E. coli reference, and
BLF must sum to exactly 16,247 before E1 starts; any other sum is input drift.

**Stop, cleanup, and rollback:** stop on any global stop condition below.
Terminate no healthy peer merely to hide partial success; preserve each
process's terminal state and evidence. Remove only unneeded caller-owned
temporary outputs after audit and retention decisions. Store rollback is
forward-only: retain valid immutable entries, refresh the baseline, and resume
only through a reviewed later execution.

## Separate dbCAN Gate

dbCAN is not part of the eggNOG/InterPro expansion. Its current approved gate
is the [2026-08-19 dbCAN target-Store execution
plan](20260819-v1.0-dbcan-target-store-execution-plan.md), which places dbCAN
before InterPro and fixes the released SeqEvi 0.3.1 digest-pinned managed kit.
That plan owns dbCAN-specific Preflight, supported setup, fixed 50-record target
execution, 2,729-record one-process sizing, the small same-key ClaimSession
disposition, selected expansion, Docker cleanup and replay criteria. D1 must
prove the host/bridge deployment mapping to one exact service/database/Store.
D2 derives ResourceID independently from the validated lock, probes the exact
released image's ToolRuntimeDigest read-only after acquisition, and repeats
exact-key inventory before D3; setup JSON is not treated as a ResourceID source.

The dbCAN lane starts at one file/process and 64 adapter threads, cannot inherit
headroom or scaling claims from eggNOG or InterPro, and never deletes Store data
to manufacture cold work. A post-sizing different-file gate may start two
processes at 32 threads each; two at 64 is allowed only after its measured CPU,
RSS, scratch, Ceph I/O, Store/CAS and two-process headroom gate passes. Only a
predeclared, naturally all-missing 2,729-key D4 run supplies those rates; a
partial or replay-only D4 keeps expansion closed. Current managed-v2 starts OCI
before ClaimSession, so D5 is recorded unavailable and makes no same-key managed
gate claim. Hidden image injection, a generic runner and full repeated
per-adapter C1/C2 are not part of the approved dbCAN review unit.

## Global Stop Conditions

Stop the active adapter family, preserve evidence, and do not advance
automatically when any of the following occurs:

- replay computes one or more records;
- ToolRuntimeDigest, ResourceID, profile, semantic parameters, or input digest
  drifts;
- a DuckDB schema, table, row count, or input-map check fails;
- the applicable backup/export boundary is not satisfied, including incomplete
  fresh coordinated backup readback when the Store is production authority;
- Store health fails or any upload/commit returns an error;
- an internal adapter timeout or external containment watchdog expires, a
  process deadlocks, or the OS-owned containment boundary is not empty after
  the frozen deadline plus 60-second termination grace;
- an unexplained conflict, Store delta, or payload inequality appears;
- a CAS artifact is missing/duplicated/corrupt or upload temporary residue
  remains; or
- measured PostgreSQL, artifact, or project-root capacity provides less than
  2x projected headroom for a declared successor, or less than its
  Preflight-frozen final reserve at the last slice.

Recovery begins with an operator-reviewed incident summary, fresh Store and
output readback, and a decision whether the same slice is safe to resume. Never
erase immutable Store state or overwrite evidence to force a clean rerun.

## Final Completion Criteria

Cache seeding is complete only when:

1. the refreshed 50-file manifest accounts for every intended corpus input and
   retains the validated InputID/sequence properties;
2. the serial sanity and sizing slice has accepted cold/replay evidence and
   adapter-specific capacity measurements for eggNOG and InterPro-Pfam;
3. concurrency C1 and C2 have accepted eggNOG and InterPro-Pfam results,
   including demonstrated same-key contention rather than visibility alone;
4. every approved expansion slice has accepted output and zero-compute
   independent replay for both adapter families;
5. final Store rows, referenced and unreferenced artifact digests/bytes,
   health, capacity, and temporary residue have been independently read back
   and reconciled to missing-key unions and all uploaded artifacts; final
   project-root capacity reconciles every retained result and peak scratch
   observation;
6. every DuckDB output is present, digest-recorded, schema-correct, and mapped
   to its manifest inputs, with JSON summaries and resource-use evidence;
7. sanitized accepted summaries are retained under the Store sibling
   `seed-runs` area and the final reproducible measurement is published in the
   named future benchmark destination; and
8. failures, exclusions, dbCAN readiness, and unseeded identities are stated
   explicitly rather than converted into success.

Completion is execution evidence to be written later; this document remains
an execution plan until those criteria are actually met.

## Future Automation Decision

The first gates require no repository code change. If repeated execution shows
that interruption recovery or slice bookkeeping needs a resumable runner,
create a separate AO issue and pull request. Its design must be generic and
Store/API-based, preserve the public identity and output contracts, and avoid
Ceph-, corpus-, project-, or host-specific domain coupling.

## Review And Delivery

The Issue #55 documentation rebaseline is one review unit: the dated dbCAN
execution plan, these current-continuation updates and the navigation changes
in `docs/README.md`. Runtime execution, benchmark evidence, the narrow Docker
attach-timeout cleanup test prerequisite, runbook changes and any future runner
are separate review units. Documentation delivery runs repository
link/status/secret checks and the canonical `pdm run check`; no annotation,
Store mutation, image pull or runtime code change is part of the plan-writing
change.

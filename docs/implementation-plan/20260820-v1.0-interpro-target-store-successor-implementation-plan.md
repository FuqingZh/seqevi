# InterPro Target-Store Successor Implementation Plan

Version: v1.0
Date: 2026-08-20
Status: approved execution successor; read-only Preflight active
Target release: SeqEvi 0.3.1
Issue: #62
Continuation of: [shared Store cache-seeding implementation
plan](20260807-v1.0-shared-store-cache-seeding-implementation-plan.md)

## Authority And Delta Scope

This narrow successor controls only the remaining InterPro/Pfam target-Store
lane. The eggNOG lane and scoped dbCAN gate are already closed by their
[eggNOG](../benchmarks/20260818-v1.0-eggnog-target-store-closeout.md) and
[dbCAN](../benchmarks/20260819-v1.0-dbcan-target-store-closeout.md) records;
neither is a prerequisite to repeat. All master-plan input, backup/export,
capacity, Store/CAS reconciliation, immutable-forward recovery, output,
retention, and global stop rules remain in force unless this document states
an InterPro-specific delta.

The execution identity is the released `seqevi==0.3.1` distribution and named
`interpro-pfam-38.1` profile. Preflight must resolve and freeze the profile's
actual adapter contract, ToolRuntimeDigest, ResourceID, canonical semantic
parameters, InterProScan/Pfam/JDK runtime components, and read-only resource
path. Historical expected identities are the accepted InterProScan 5.77-108.0,
Pfam 38.1 runtime digest
`sha256:108b8fb3735c166fd30c3a5189240b40acee66f3200107b2967cd16728afefd4`
and ResourceID
`interpro/108.0/pfam/38.1/sha256:ffdda753ae10b2942abab6e364dcc2a8ab16d3df1ff9f382692ec4d367a64860`.
Any mismatch stops Preflight; it is not silently refreshed.

Affected repository documents are this successor, the documentation index,
the master plan's continuation pointer, and—only after a gate passes—a dated,
sanitized benchmark record. Raw evidence stays outside the repository under a
fresh `/home/fqzhang/tmp/seqevi-seed-<run-id>`.

## Fixed Reuse And Prohibitions

Execution reuses released SeqEvi boundaries rather than reproducing them:

- `stage_fasta` supplies canonical validation, InputID handling, SequenceID
  identity and the staged input digest; FASTA is never hand-parsed.
- The resolved `AdapterContract.evidence_key` constructs every target
  EvidenceKey; identities are never hand-constructed.
- Released Store `lookup_many` and artifact fetch APIs perform bounded,
  read-only inventory and payload checks; existing PostgreSQL/CAS audit logic
  supplies schema, row/count/digest, reference, residue and filesystem
  reconciliation.
- Any later authorized execution uses `seqevi annotate --json`; result
  validation uses `scan_annotations`, `sha256_file`, DuckDB queries and Polars
  expressions against the adapter-native result schema.
- The accepted [containment harness
  record](../benchmarks/20260819-v1.0-interpro-preflight-containment-harness-acceptance.md)
  and its exact source lineage are reused. Preflight freezes a finite InterPro
  internal `--timeout-seconds` and a strictly longer external watchdog; it
  cannot inherit eggNOG's numeric deadlines or weaken the accepted cleanup.

This successor explicitly prohibits product API, adapter contract, evidence
identity or Store schema changes; a generic or resumable seeding runner; a new
scheduler or dependency; copied per-gate runners; private contention hooks;
manual Store/CAS writes; and merging adapter-native Pfam output into another
annotation schema. Host paths and the evidence root remain operator evidence,
not product configuration or domain-model contracts.

## Read-Only Preflight

Preflight is the only active slice. It performs no annotation, ClaimSession
operation, Store commit, CAS upload, profile publication, resource mutation or
Store maintenance.

Using bounded, non-recursive discovery from the accepted corpus and host
evidence, it must:

1. Freeze the intended 50-file corpus as a sorted manifest with exact path,
   size, file SHA-256, record and unique SequenceID counts, and sorted
   SequenceID-set digests. Use `stage_fasta`; aggregate agreement with the old
   planning snapshot is a check, not proof of byte continuity.
2. Record the exact released SeqEvi package/source identity and resolve
   `interpro-pfam-38.1`. Freeze the accepted profile, runtime, resource and
   semantic identity plus read-only resource evidence.
3. Freeze fresh target `/health`, capabilities, schema revision, PostgreSQL
   row/count/digest and database-size baseline, CAS row/file/byte/inventory
   baseline, coordination-row baseline, and service/database/artifact-root
   mapping using the existing audits. Record the applicable historical
   export/readback precondition; stop if the Store has become production
   authority without the master plan's completed identity-bearing backup.
4. Record PostgreSQL, CAS filesystem and project/output-root availability,
   memory/CPU and relevant scratch/I/O baselines. Freeze operator-approved
   minimum final reserves for PostgreSQL, CAS, project/output space and memory;
   Preflight records availability but makes no growth or concurrency claim.
5. Freeze exact sanity, C1-A, C1-B, C2-BLF and C2-UniProt inputs from the
   manifest. Prove the master-plan disjointness rules and the C2 9,115-key
   subset/9,116-key union relationship with canonical SequenceID sets.
6. Derive all keys with the resolved `AdapterContract.evidence_key`, inventory
   them through bounded Store lookups, and freeze each set's present/missing
   counts and digests. Select exactly one honest key-state branch for every
   gate: all-missing, all-present, or partial.
7. Freeze a finite InterPro internal timeout from accepted runtime evidence and
   the exact planned input bounds, a strictly longer watchdog, cleanup grace,
   containment command lineage and empty-boundary checks. Non-finite,
   non-positive, mismatched or inverted deadlines fail closed.
8. Freeze isolated project, DuckDB, log and JSON paths for sanity, C1 and C2;
   prove none aliases another, the Store artifact root or the resource tree.

Preflight passes only when all readbacks are authoritative, mutually
consistent, digest-recorded and sanitary. Before/after Store and CAS snapshots
must be identical. A sanitized Preflight benchmark may be committed only after
that complete readback; otherwise preserve raw evidence and report the exact
gap without guessing or starting annotation.

## Key-State Branches

The selected branch is refreshed immediately before any later authorized
mutating gate:

- **All-missing:** the exact frozen set may run the gate's cold/concurrent
  shape after its predecessor and capacity gates pass. Expected Store evidence
  growth is the missing-key union.
- **All-present:** run only independent complete-input `annotate --json`
  replays; require every key cached, zero computation and exact zero
  PostgreSQL/CAS growth. Do not claim cold sizing or contention.
- **Partial:** digest disjoint present and missing subsets, validate/fetch the
  present payloads, and run the unchanged complete inputs so only the frozen
  missing union computes. Reconcile predicted per-process cache/computed
  counts and Store growth, then require independent zero-compute replay. Do
  not label the result cold.

No branch deletes accepted evidence to manufacture misses. A legitimate
partial state is forward progress; unknown or drifting state is a stop.

## Gate Graph And Acceptance

```text
read-only Preflight
  -> sanity and independent replay
  -> C1 disjoint two-process gate and independent replay
  -> C2 overlapping-key gate and independent replay
  -> E1 -> E2 -> E3 -> E4 -> E5 -> E6
  -> final reconciliation and InterPro closeout
```

Every gate inherits the master plan's exact inputs, at-most-two-file shape,
one-adapter-family boundary, fresh identity/health/key-state/capacity checks,
isolated projects and adapter-native DuckDB/JSON validation. Every output is
opened read-only with `scan_annotations`, checked by DuckDB/Polars for schema,
tables, input map, hit/no-hit and row cardinalities, and hashed with
`sha256_file`. Store/CAS deltas and all referenced or unreferenced artifact
rows/files are reconciled by the existing PostgreSQL/CAS audits.

- **Sanity:** use the frozen 2,729-record Lacticaseibacillus input. An
  all-missing or partial result supplies measured InterPro growth, retained
  output and peak-scratch rates; all-present supplies replay acceptance only.
  Require zero-compute replay and defensible 2x headroom for exact C1 inputs.
- **C1:** use the two Preflight-frozen disjoint sets, excluding sanity and C2
  keys. Both processes must complete without health, HTTP, commit, schema,
  identity, residue or isolation defects. A cold/partial branch reconciles the
  missing union and establishes measured two-process rates; all-present is
  replay-only. Require 2x headroom for C2.
- **C2:** use the frozen BLF and UniProt subset pair. No private post-lookup
  barrier or hook may manufacture contention. Cold/partial acceptance requires
  observable supported-path overlap, missing-union growth, equal immutable
  payloads and later zero-compute replays. Visibility-only or all-present
  execution is recorded honestly and does not claim same-key contention.
- **E1-E6:** retain the master plan's exact cumulative ordering and selection
  gates. E1 excludes the redundant C2 UniProt file and must total exactly
  16,247 selected records. Each slice freezes exact files, runs no more than
  two, reconciles its selected branch and independent replay, and retains 2x
  measured headroom for its declared successor. E6 instead must finish above
  every Preflight-frozen final reserve.

An all-present or partial sanity/C1/C2 branch cannot invent the cold sizing or
contention evidence required by a later gate. Stop at the first missing
predecessor and record the narrower accepted result.

## Stop, Cleanup And Recovery

Stop and preserve evidence on package, source, profile, ToolRuntimeDigest,
ResourceID, semantic, input, EvidenceKey, schema or containment-lineage drift;
failed health/backup boundary; timeout; non-empty owned process boundary;
DuckDB/JSON or input-map defect; HTTP/commit error; unexplained Store/CAS
delta; missing/corrupt artifact; upload residue; payload inequality; or loss of
the applicable headroom/final reserve.

On internal timeout, allow the accepted SeqEvi `ToolRunner` path to terminate
and reap its adapter group. On outer-watchdog expiry, propagate termination
through the accepted bridge, apply the frozen bounded TERM/KILL cleanup, and
prove both nested and outer owned boundaries empty. Preserve primary and
cleanup errors distinctly. Remove only invocation-owned temporary material
after audit; never delete immutable Store rows or CAS artifacts, overwrite a
result, or retry an external write with unknown outcome before authoritative
readback proves the intended state absent.

Raw evidence remains under the run root. Only small sanitized manifests,
summaries and audit outputs may enter a dated benchmark record after a gate is
accepted; credentials, raw environments, unredacted service/database
configuration and private host topology stay excluded.

## Compact Delivery Splits

1. **Plan and read-only Preflight:** this successor/index/master pointer, full
   local gate, then a separately committed sanitized Preflight benchmark only
   if every authoritative readback passes.
2. **Sanity/C1/C2:** one review unit for the selected branches, measured
   capacity, native results, Store/CAS audits, containment and replays; stop if
   cold sizing or contention prerequisites remain unproven.
3. **E1-E6 and closeout:** small contiguous expansion records, ending with
   final Store/CAS/result/capacity reconciliation and one InterPro closeout.

Each split runs `pdm run check`, receives exact-current-head normal review and
retains zero unresolved threads before merge eligibility. No split changes the
product contract or folds closed eggNOG/dbCAN work back into the active lane.

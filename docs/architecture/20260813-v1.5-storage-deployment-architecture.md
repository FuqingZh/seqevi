# Storage Deployment Architecture

Version: v1.5

Date: 2026-08-13

Status: approved target architecture

Supersedes: [v1.4](20260813-v1.4-storage-deployment-architecture.md)

SeqEvi retains the v1.4 revision-0004 schema, sweepers, SQLite teardown and
target resolution, fencing, migration, preservation, and deployment contracts.
PostgreSQL maintenance acquisition budgets are unchanged and remain owned by
issue #34.

Each `HttpEvidenceStore` privately owns one long-lived asyncio event-loop
thread and one `httpx.AsyncClient` connection pool for all ClaimSession-owned
HTTP operations. Construction is internal and deterministic; there is no
public client or async-client factory. Tests may supply an underscore-prefixed
transport seam, which does not transfer lifecycle ownership. Store close first
cancels and gathers every outstanding request task, closes the async client,
stops its loop, and joins its thread. Request deadline cancellation is
task-local, awaits transport cleanup, and preserves concurrent requests and
sessions using the same pool. Artifact file reads run outside the transport
event-loop thread so local filesystem latency cannot suspend deadlines or
heartbeats. A separate per-session pool is not needed because active
cancellation and isolation pass on the shared pool.

Released non-session operations continue through the synchronous client and
retain their v0.2.0 behavior. The private async boundary adds no public async
Store method, deployment component, service, timing knob, or connection-pool
configuration.

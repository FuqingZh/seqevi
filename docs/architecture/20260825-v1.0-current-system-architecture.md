# SeqEvi Current System Architecture

Version: v1.0

Date: 2026-08-25

Status: approved target architecture; current overview

Applies to: SeqEvi 0.4.0 release-candidate source

Release-preparation note (2026-09-01): merged R2 implements native OCI shared
artifacts and schema 0005 under storage v1.8 as a compatible new public
capability. The source version is now 0.4.0, but no SeqEvi 0.4.0 Python or
GitHub release, production deployment, or historical-byte migration has
occurred.

This overview is the current front door. It consolidates the implemented
product shape and routes readers to the detailed contracts; it does not
replace their scientific identity or compatibility rules.

## Product Boundary

SeqEvi is an independent, content-addressed protein-sequence evidence cache.
It reuses annotation evidence only when the complete immutable key matches:

```text
SequenceID
AdapterContractVersion
ToolRuntimeDigest
ResourceID
CanonicalSemanticParameters
```

One invocation selects one official adapter and publishes one self-describing
DuckDB result. Adapter-native schemas remain separate. Caller-owned annotation
databases remain outside SeqEvi and are never downloaded, copied, or bundled by
the scientific core.

The three official adapters are:

- `eggnog` for eggNOG-mapper;
- `interpro-pfam` for InterProScan/Pfam; and
- `dbcan-cazyme` for protein-level dbCAN CAZyme annotation.

## Public Surfaces

- Python: `seqevi.annotate(...)` and `seqevi.scan_annotations(...)`.
- CLI: `annotate`, `resource`, `profile`, `setup`, `serve`, and the four
  acknowledgement-bound `store-maintenance-*` commands.
- Result: one immutable DuckDB file containing adapter-native relations,
  sequence mapping, no-hit rows, metadata, semantic parameters, and run
  metrics.
- Store: local SQLite/POSIX or passive shared HTTP/PostgreSQL storage, with
  legacy POSIX and an explicit native OCI Registry backend for new shared
  artifacts. Local operation has no ORAS or zot dependency.

Direct adapter execution requires Python 3.12 or newer on Linux. `ToolRunner`
uses Linux `/proc` and `waitid` to fail closed while terminating and reaping an
external tool process group.

## Runtime And Distribution

Host profiles remain supported for all three adapters. Managed `seqevi setup`
is currently available only for `dbcan-cazyme`. It validates a caller-owned
four-file resource and delegates annotation to a digest-pinned first-party OCI
image. Docker remains outside the scientific domain model and is invoked only
at the application distribution edge.

The managed container receives narrow input, resource, output, and optional
Store mounts. Local-Store execution uses no network. An explicit shared
HTTP(S) Store enables container networking. Managed eggNOG and InterPro are not
implemented.

The bundled/selectable dbCAN kit and its digest remain unchanged. An immutable
0.3.5 revision image was automatically published from merged revision
`f7781c4ce7d642ef46619e6f02c7be3745803ca4` at
`ghcr.io/fuqingzh/seqevi-dbcan@sha256:1914939f1776fee3faac5241fc84f99f4534f37e20cc4d4d48eedf491c38488a`.
It is a publication record, not a newly registered managed kit or a 0.4.0
release. Image publication now requires an explicit workflow dispatch;
ordinary main and source pushes do not publish images.

## Release Channels And Version Identity

The package version is dynamic SCM metadata. Only an exact canonical
`vX.Y.Z` tag produces final installed version `X.Y.Z`. Untagged source derives
the next-minor PEP 440 development version from the most recent canonical tag,
commit distance, and source revision. Installed distribution metadata is the
runtime authority for both `seqevi.__version__` and CLI `--version`; no static
source version is maintained.

The delivery states are intentionally separate:

| State | Authority and effect |
| --- | --- |
| CI | pull requests and ordinary pushes validate repository behavior; no publication |
| nightly | schedule or manual dispatch validates exact clean `main`, then retains one SHA-named wheel/sdist artifact for 14 days; no package-index or OCI publication |
| tag candidate | canonical `vX.Y.Z` push validates immutable source and distributions; never publishes |
| GitHub Release | publishing a stable, non-prerelease release for the exact tag authorizes the PyPI workflow to begin after fail-closed identity validation |
| PyPI | a separate `pypi` environment job uses Trusted Publishing to upload only the exact verified artifact; completion requires external project/version/file readback |
| dbCAN OCI | independent manual dispatch publishes exact-source revision tags and verifies the pushed digest; Python release events do not trigger it |

GitHub Release publication precedes eventual PyPI completion and is not proof
that PyPI accepted the files. TestPyPI and prerelease/RC publication are not
supported channels. No channel implicitly deploys SeqEvi, deploys a Registry,
migrates data, changes the bundled managed kit, or authorizes another channel.

## Shared-Store Coordination

A claim-capable annotation opens one invocation-scoped `ClaimSession`.
ClaimSession suppresses duplicate active work, renews one session authority,
fences exact-key generations, and reconciles ambiguous finalization against
immutable terminal evidence. The shared Store remains passive and never
schedules annotation tools.

The internal route set is:

```text
/v1/internal/claim-sessions/{capabilities,open,renew,acquire,authority,finalize,close}
```

Released v0.2.0 clients retain the legacy lookup/fetch/commit surface against
legacy mode; OCI mode requires upgraded clients via capability admission. A legacy
commit may win against an active ClaimSession under the same exact-key lock;
the transaction retires the claim and immutable convergence prevents overwrite.

One public finalize call first stages uncached artifacts with independent
900-second total deadlines and at most 60-second HTTP phase timeouts. It then
captures fresh authority and one short metadata deadline shared by every split
batch and reconciliation. Split batches do not receive new finalize budgets.
Service upload filesystem operations run off the event loop under bounded
admission; cancellation retains ownership until I/O and cleanup finish.
OCI staging delegates transfer to pinned ORAS. OCI metadata finalize verifies
canonical manifest and blob descriptors without downloading the large layer;
download still fully checks raw size and SHA-256. Existing registered POSIX
bytes stay in place and local execution requires no Registry or ORAS.

## Current Contract Map

| Boundary | Current authority |
| --- | --- |
| sequence identity, evidence and ClaimSession | [sequence evidence v1.6](20260814-v1.6-sequence-evidence-contract.md) |
| adapters and external tools | [adapter v1.3](20260821-v1.3-adapter-contract.md) |
| DuckDB result | [result consumption v1.1](20260804-v1.1-result-consumption-contract.md) |
| host execution profiles | [profile v1](20260724-v1.0-execution-profile-contract.md) |
| managed OCI profiles | [profile v2.2](20260806-v2.2-execution-profile-contract.md) |
| local/shared Store and deployment | [storage v1.8](20260831-v1.8-storage-deployment-architecture.md) |
| managed dbCAN distribution | [managed distribution v1.2](20260806-v1.2-managed-adapter-distribution-architecture.md) |
| progress | [progress v1.1](20260824-v1.1-progress-observability-contract.md) |
| validation | [validation strategy v1.4](../testing/20260825-v1.4-validation-strategy.md) |

Higher-version documents inherit the unchanged rules they name from historical
layers. Historical documents are navigation evidence, not alternate current
authority; use [the documentation index](../README.md) and
[archive index](../archive/README.md) to resolve them.

## Known Incomplete Work

- The original public-user Slice D acquisition run remains incomplete and is
  not represented as a pass.
- Managed dbCAN has no claim-before-OCI path, so its D5 same-key managed gate is
  unavailable.
- InterPro v2 target-Store identity/exact-key refresh remains open before
  target-Store sanity.
- Global cache seeding remains incomplete.
- Public progress protocols, ETA, and multi-input scheduling remain later work.
